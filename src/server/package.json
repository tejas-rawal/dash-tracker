{
  "name": "server",
  "private": true,
  "version": "0.0.1",
  "description": "Dash bus tracker server",
  "keywords": [],
  "scripts": {
    "start": "NODE_ENV=production node app.js",
    "test": "npm run lint && npm run custom-tests && echo 'Done.'",
    "lint": "./node_modules/eslint/bin/eslint.js . --max-warnings=0 --report-unused-disable-directives && echo '✔  Your .js files look good.'"
  },
  "main": "app.js",
  "repository": {
    "type": "git",
    "url": "git://github.com/tejas/server.git"
  },
  "author": "Tejas Rawal",
  "license": "Apache-2.0",
  "engines": {
    "node": "^16.16"
  },
  "dependencies": {
    "@types/express": "^5.0.0",
    "express": "^4.21.2",
    "nodemon": "^3.1.9"
  }
}
