---
phase: 01-consolidate-lint-format-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - biome.json
  - bun.lockb
  - package-lock.json
  - .prettierrc
  - .prettierignore
  - .vscode/settings.json
  - .vscode/extensions.json
autonomous: true
requirements: [TOOL-01, TOOL-02, TOOL-03]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "package.json format and format:write scripts both invoke Biome (biome check . / biome check . --write) — no prettier invocation remains in any script"
    - "package.json has no top-level prettier field and no prettier, @jonahsnider/prettier-config, or prettier-plugin-packagejson entries in dependencies or devDependencies"
    - "bun.lockb and package-lock.json no longer reference prettier, @jonahsnider/prettier-config, or prettier-plugin-packagejson"
    - "No Prettier config or ignore files remain in the repo — .prettierrc and .prettierignore do not exist"
    - "No checked-in tooling file still names Prettier as the active formatter — .vscode/settings.json editor.defaultFormatter is biomejs.biome and .vscode/extensions.json recommendations has no esbenp.prettier-vscode"
    - "biome.json formatter settings (120 line width, 4-space indent, double quote style) are unchanged from before the swap, and files.ignore no longer contains **/package.json"
    - "bun run lint, bun run format, and bun run build all exit 0 after the swap"
  artifacts:
    - package.json
    - biome.json
    - bun.lockb
    - package-lock.json
    - .vscode/settings.json
    - .vscode/extensions.json
  key_links:
    - "package.json scripts.format / scripts.format:write -> biome CLI invocation (biome check .)"
    - "biome.json files.ignore -> package.json now covered by biome check . like every other file (D-01)"
    - ".vscode/settings.json editor.defaultFormatter -> biomejs.biome extension"
    - "package.json devDependencies -> bun.lockb and package-lock.json consistency"
  # Prohibition probe (recall -> precision, per prohibition-probe.md) ran over TOOL-01/02/03. Stage 1
  # candidates were exclusively routine-engineering completeness concerns (stale doc/CI references,
  # script-name breakage) — this dev-tooling config phase has no user-facing surface and no
  # values/safety/fairness/privacy/transparency dimension. Stage 2 precision correctly yields zero
  # kept prohibitions (clean-utility outcome).
  prohibitions: []
---

<objective>
Make Biome the sole lint/format tool for dash-tracker. Remove Prettier entirely — dependency, config files, plugin, and every checked-in tooling reference — and rewire `package.json`'s `format`/`format:write` scripts to the Biome-only flow, per CONTEXT.md decisions D-01 through D-04.

Purpose: closes TOOL-01, TOOL-02, TOOL-03 — a single, unambiguous command for linting and formatting with no drift between what CI checks and what a contributor runs locally.
Output: `package.json` and `biome.json` rewired to a Biome-only toolchain; `bun.lockb`/`package-lock.json` regenerated; `.prettierrc`/`.prettierignore` deleted; `.vscode/settings.json`/`.vscode/extensions.json` no longer reference Prettier.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-consolidate-lint-format-tooling/01-CONTEXT.md
@.planning/phases/01-consolidate-lint-format-tooling/01-PATTERNS.md
@package.json
@biome.json
@.prettierrc
@.prettierignore
@.vscode/settings.json
@.vscode/extensions.json
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Swap package.json + biome.json to Biome-only lint/format, regenerate lockfiles</name>
  <files>package.json, biome.json, bun.lockb, package-lock.json</files>
  <read_first>package.json, biome.json, .planning/phases/01-consolidate-lint-format-tooling/01-CONTEXT.md, .planning/phases/01-consolidate-lint-format-tooling/01-PATTERNS.md</read_first>
  <action>
In package.json: delete the top-level "prettier": "@jonahsnider/prettier-config" key entirely (per D-01/D-02 discussion, this field only steers Prettier and has no Biome equivalent).
Change the "format" script value from "prettier --check ." to "biome check .".
Change the "format:write" script value from "prettier --check . --write" to "biome check . --write".
Leave "lint" ("biome check .") and "lint:fix" ("biome check . --apply-unsafe") exactly as they are — they are already the target pattern the new format scripts mirror.
Remove exactly three devDependencies entries: "@jonahsnider/prettier-config", "prettier", "prettier-plugin-packagejson" (per TOOL-02). Leave every other dependency and devDependency entry untouched, in place, unmodified.
In biome.json: change the files.ignore array from ["node_modules", "dist", "coverage", "**/package.json"] to ["node_modules", "dist", "coverage"] — remove only the "**/package.json" entry, per D-01, so biome check --write covers package.json like every other file (closing the gap left by dropping prettier-plugin-packagejson).
Do not touch any other biome.json key. The formatter block (lineWidth 120, indentStyle "space", indentWidth 4), javascript.formatter.quoteStyle "double", json.parser.allowComments, and the entire linter block already match what Prettier previously enforced — this is a tooling swap, not a style change, so these values stay byte-identical.
Run `bun install` at the repo root to regenerate bun.lockb reflecting the three removed devDependencies.
Run `npm install` at the repo root to regenerate package-lock.json reflecting the same removals, per D-04 — package-lock.json is kept for npm compatibility and must not go stale relative to package.json.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');if(p.prettier)process.exit(1);if(p.scripts.format!=='biome check .')process.exit(1);if(p.scripts['format:write']!=='biome check . --write')process.exit(1);const dd=p.devDependencies||{};if(dd.prettier||dd['@jonahsnider/prettier-config']||dd['prettier-plugin-packagejson'])process.exit(1);if(p.scripts.lint!=='biome check .'||p.scripts['lint:fix']!=='biome check . --apply-unsafe')process.exit(1);console.log('package.json OK')" && node -e "const b=require('./biome.json');if(b.files.ignore.includes('**/package.json'))process.exit(1);if(b.formatter.lineWidth!==120||b.formatter.indentStyle!=='space'||b.formatter.indentWidth!==4)process.exit(1);if(b.javascript.formatter.quoteStyle!=='double')process.exit(1);console.log('biome.json OK')" && ! grep -a -qi prettier bun.lockb && ! grep -qi prettier package-lock.json && bun run lint && bun run format && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - package.json "format" script equals "biome check ."
    - package.json "format:write" script equals "biome check . --write"
    - package.json has no top-level "prettier" key
    - package.json "lint" and "lint:fix" scripts are unchanged ("biome check ." / "biome check . --apply-unsafe")
    - package.json devDependencies has no "prettier", "@jonahsnider/prettier-config", or "prettier-plugin-packagejson" keys
    - biome.json files.ignore array no longer contains "**/package.json"
    - biome.json formatter block (lineWidth 120, indentStyle "space", indentWidth 4) and javascript.formatter.quoteStyle "double" are byte-identical to the pre-task values
    - bun.lockb contains no reference to "prettier" (checked via grep -a)
    - package-lock.json contains no reference to "prettier"
    - bun run lint exits 0
    - bun run format exits 0 (now includes package.json in its coverage)
    - bun run build exits 0
  </acceptance_criteria>
  <reversibility rating="reversible">Config/dependency edits and lockfile regeneration are fully undone by `git checkout -- package.json biome.json bun.lockb package-lock.json` followed by `bun install`; no data migration, no external service state, no irreversible side effect.</reversibility>
  <done>package.json's format/format:write scripts invoke Biome and its prettier field/devDependencies are gone; biome.json's files.ignore no longer excludes package.json while its formatter values are unchanged; both lockfiles are regenerated and free of prettier references; lint, format, and build all pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove Prettier config files and stale editor/tooling references</name>
  <files>.prettierrc, .prettierignore, .vscode/settings.json, .vscode/extensions.json</files>
  <read_first>.prettierrc, .prettierignore, .vscode/settings.json, .vscode/extensions.json, .planning/phases/01-consolidate-lint-format-tooling/01-CONTEXT.md, .planning/phases/01-consolidate-lint-format-tooling/01-PATTERNS.md</read_first>
  <action>
Delete .prettierrc entirely. Its only content is {"plugins": ["prettier-plugin-packagejson"]} — no replacement is needed; Biome's json formatter settings in biome.json already cover JSON formatting.
Delete .prettierignore entirely. Its final block documented the "**/package.json" carve-out that Task 1 already closed by editing biome.json's files.ignore; .gitignore already independently covers the build-artifact/OS-file entries it also listed.
In .vscode/settings.json: change the top-level "editor.defaultFormatter" value from "esbenp.prettier-vscode" to "biomejs.biome" (per D-02) — this is the bare top-level key, not one of the per-language blocks. Remove the six now-fully-redundant per-language override blocks ("[javascript]", "[javascriptreact]", "[json]", "[jsonc]", "[typescript]", "[typescriptreact]"), since each already set "editor.defaultFormatter": "biomejs.biome" and the corrected global default now matches every one of them. The resulting file keeps exactly three keys: "editor.defaultFormatter": "biomejs.biome", "typescript.preferences.importModuleSpecifierEnding": "js", "typescript.tsdk": "node_modules/typescript/lib".
In .vscode/extensions.json: remove "esbenp.prettier-vscode" from the recommendations array (per D-03), leaving ["editorconfig.editorconfig", "redhat.vscode-yaml", "biomejs.biome"]. Preserve the existing order of the remaining three entries.
  </action>
  <verify>
    <automated>test ! -f .prettierrc && test ! -f .prettierignore && node -e "const s=require('./.vscode/settings.json');if(s['editor.defaultFormatter']!=='biomejs.biome')process.exit(1);if(JSON.stringify(s).includes('esbenp.prettier-vscode'))process.exit(1);console.log('settings.json OK')" && node -e "const e=require('./.vscode/extensions.json');if(e.recommendations.includes('esbenp.prettier-vscode'))process.exit(1);if(!e.recommendations.includes('biomejs.biome'))process.exit(1);console.log('extensions.json OK')"</automated>
  </verify>
  <acceptance_criteria>
    - .prettierrc does not exist
    - .prettierignore does not exist
    - .vscode/settings.json "editor.defaultFormatter" equals "biomejs.biome"
    - .vscode/settings.json contains no occurrence of "esbenp.prettier-vscode" anywhere in the file
    - .vscode/settings.json retains "typescript.preferences.importModuleSpecifierEnding": "js" and "typescript.tsdk": "node_modules/typescript/lib" unchanged
    - .vscode/extensions.json "recommendations" array does not contain "esbenp.prettier-vscode"
    - .vscode/extensions.json "recommendations" array still contains "editorconfig.editorconfig", "redhat.vscode-yaml", and "biomejs.biome"
  </acceptance_criteria>
  <reversibility rating="reversible">Deleted files and edited JSON are fully recoverable via `git checkout -- .prettierrc .prettierignore .vscode/settings.json .vscode/extensions.json`; no destructive action outside git's tracked history.</reversibility>
  <done>.prettierrc and .prettierignore are deleted; .vscode/settings.json's global default formatter is biomejs.biome with the redundant per-language overrides removed and no remaining Prettier reference; .vscode/extensions.json no longer recommends the Prettier extension.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts This Phase Produces

Every symbol/script/config-key this phase creates, changes, or removes, so downstream drift verification does not flag them as unexpected:

- `package.json` `scripts.format` — value changed from `"prettier --check ."` to `"biome check ."`
- `package.json` `scripts.format:write` — value changed from `"prettier --check . --write"` to `"biome check . --write"`
- `package.json` top-level `prettier` key — removed entirely
- `package.json` `devDependencies.prettier` — removed
- `package.json` `devDependencies["@jonahsnider/prettier-config"]` — removed
- `package.json` `devDependencies["prettier-plugin-packagejson"]` — removed
- `biome.json` `files.ignore` — `"**/package.json"` entry removed (array becomes `["node_modules", "dist", "coverage"]`)
- `bun.lockb` — regenerated via `bun install`; no longer resolves the 3 removed packages
- `package-lock.json` — regenerated via `npm install`; no longer resolves the 3 removed packages
- `.prettierrc` — file deleted
- `.prettierignore` — file deleted
- `.vscode/settings.json` top-level `editor.defaultFormatter` — value changed from `"esbenp.prettier-vscode"` to `"biomejs.biome"`
- `.vscode/settings.json` per-language override blocks (`[javascript]`, `[javascriptreact]`, `[json]`, `[jsonc]`, `[typescript]`, `[typescriptreact]`) — removed as redundant now that the global default matches every one of them
- `.vscode/extensions.json` `recommendations` — `"esbenp.prettier-vscode"` entry removed (array becomes `["editorconfig.editorconfig", "redhat.vscode-yaml", "biomejs.biome"]`)

No new scripts, keys, or files are introduced beyond the modifications listed above. `lint`, `lint:fix`, `build`, `test`, `test:coverage`, `start-server`, `dev-server` scripts, `biome.json`'s formatter/linter blocks, and every `dependencies`/`devDependencies` entry outside the 3 removed ones are unchanged.
</artifacts_produced>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe fallback)

No SPEC.md exists for this phase, so the deterministic edge-probe classified all three requirements as unresolved (unclassified / concurrency — a poor taxonomy fit for a config-swap phase). Per the spec-less fallback protocol, each is surfaced here as an explicit assumption rather than silently resolved or dropped:

- **TOOL-01** (probe: unclassified) — Assuming "Biome is the sole tool for both linting and formatting" means no Prettier invocation survives ANYWHERE in checked-in tooling (scripts, editor config), not narrowly just `package.json` scripts. Verified across `.vscode/settings.json` and `.vscode/extensions.json` in Task 2, not just `package.json`/`biome.json` in Task 1.
- **TOOL-02** (probe: concurrency — "If interrupted or run in parallel, what is guaranteed?") — This requirement describes a one-time, sequential, human/agent-driven migration, not a concurrent runtime process. Assuming no concurrency guarantee is needed: `bun install`/`npm install` each run once, sequentially, within Task 1, and are not invoked in parallel with themselves, each other, or Task 2.
- **TOOL-03** (probe: unclassified) — Assuming "`package.json` scripts updated to reflect the single-tool flow" means only `format`/`format:write` need rewiring, since `lint`/`lint:fix` already invoke Biome and CONTEXT.md's code_context section confirms they are the reference pattern to leave untouched (Task 1 explicitly does not modify them).

**Prohibition probe** (recall → precision, per `prohibition-probe.md`) ran over TOOL-01/02/03: Stage 1 candidates were exclusively routine-engineering completeness concerns (stale doc/CI references, script-name breakage) — this dev-tooling config phase has no user-facing surface and no values/safety/fairness/privacy/transparency dimension. Stage 2 precision correctly yields zero kept prohibitions (clean-utility outcome); `must_haves.prohibitions` is intentionally empty.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Developer/CI toolchain → repo config | `bun install` / `npm install` regenerate dependency-resolution lockfiles from the edited `package.json`; no runtime code path, API surface, or user-facing behavior is touched by this phase |

This phase removes dependencies and rewires local dev-tooling config/scripts only. No new packages are installed — `prettier`, `@jonahsnider/prettier-config`, and `prettier-plugin-packagejson` are exclusively REMOVED — so the npm/pip/cargo package-legitimacy gate does not apply: there is nothing new to vet, and no `checkpoint:human-verify` for package legitimacy is warranted.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-01-01 | Tampering | `bun.lockb` / `package-lock.json` regeneration | low | accept | Regeneration only removes 3 known devDependency entries via `bun install`/`npm install` against the already-edited `package.json` — no new dependency is added, so no supply-chain surface is introduced. Task 1's automated verify confirms both lockfiles contain no residual `prettier` references. |
| T-01-02 | Tampering | `biome.json` formatter-block parity | medium | mitigate | Accidentally changing `biome.json`'s formatter values (`lineWidth`/`indentStyle`/`indentWidth`/`quoteStyle`) while removing the `**/package.json` ignore entry would silently change project-wide code style rather than just swap tooling. Task 1's `<action>` explicitly scopes the edit to `files.ignore` only, and Task 1's `<verify>` asserts the formatter block and `quoteStyle` are byte-identical to the pre-task values, catching any accidental drift. |

No other STRIDE categories (Spoofing, Repudiation, Information Disclosure, Elevation of Privilege) apply — this phase has no authentication, request logging, data-exposure, or privilege surface; it edits static config/lockfiles in a private git repository with no runtime attack surface change.
</threat_model>

<verification>
Run at the phase level, after both tasks are complete, to confirm the full slice holds together:

1. `bun run lint` exits 0 (Biome lint, unchanged behavior).
2. `bun run format` exits 0 (Biome format-check, now covering `package.json` too).
3. `bun run build` exits 0 (`tsc` compile, unaffected by tooling swap).
4. `bun run test` exits 0 (Vitest, unaffected — confirms no accidental behavior change from the config edits).
5. `git diff package.json biome.json` shows only the changes documented in `<artifacts_produced>` — no unintended script/dependency/formatter-value edits.
6. `test ! -f .prettierrc && test ! -f .prettierignore` — both files are gone.
7. `grep -c esbenp.prettier-vscode .vscode/settings.json .vscode/extensions.json` reports 0 for both files.
</verification>

<success_criteria>
- Biome is the only tool invoked by `lint`, `lint:fix`, `format`, and `format:write` in `package.json` (ROADMAP Phase 1 Success Criterion 1 / TOOL-01, TOOL-03).
- `package.json` has no `prettier` field and no `prettier`/`@jonahsnider/prettier-config`/`prettier-plugin-packagejson` entries in `dependencies` or `devDependencies`; both lockfiles reflect the removal (Success Criterion 2 / TOOL-02).
- No Prettier config/ignore files remain, and no checked-in tooling file (editor settings/extensions) still names Prettier as the active formatter (Success Criterion 3 / TOOL-02).
- `biome.json`'s formatter settings are unchanged from their pre-phase values, confirming this is a tooling swap and not a style change (Success Criterion 4).
- `bun run lint`, `bun run format`, `bun run build`, and `bun run test` all exit 0.
</success_criteria>

<output>
Create `.planning/phases/01-consolidate-lint-format-tooling/01-01-SUMMARY.md` when done
</output>
