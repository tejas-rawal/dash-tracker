---
phase: 05-sqlite-persistence-foundation
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - package.json
  - bun.lockb
  - .gitignore
  - src/server/config/environment.ts
  - src/server/api/models/PersistedEntity.ts
  - src/server/api/models/index.ts
  - src/server/api/repositories/FavoritesRecentsRepository.ts
  - src/server/api/repositories/FavoritesRecentsRepository.test.ts
  - src/server/api/repositories/index.ts
  - src/server/app.ts
autonomous: true
requirements: [PERSIST-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The server's startup sequence initializes both BusDataRepository and the new FavoritesRecentsRepository before accepting HTTP requests, and the SIGTERM/SIGINT graceful-shutdown handler closes the SQLite connection before the process exits (Phase 5 SC #1, D-02, D-03)."
    - "A favorite row and a recent row can each be written and read back for both entity types — route and stop — through FavoritesRecentsRepository's upsertFavorite/listFavorites and upsertRecent/listRecents methods, backed by one favorites table and one recents table, not per-entity-type tables (Phase 5 SC #2, D-04)."
    - "Writing the same favorite or recent (device_id, entity_type, entity_id) twice does not create a duplicate row — the second write is a single atomic INSERT ... ON CONFLICT (device_id, entity_type, entity_id) DO UPDATE that updates the existing row's timestamp in place, rather than a read-then-write sequence (idempotency edge-probe item, D-05)."
    - "N concurrent bump-to-top writes to the same device+entity, fired via Promise.all through the repository's public async API against a real temp-file SQLite database, complete without any rejected promise and without a SQLITE_BUSY error; afterward exactly one row exists for that device+entity (concurrency edge-probe item, Phase 5 SC #3, D-06, D-07)."
    - "src/server/api/repositories/BusDataRepository.ts and BusDataRepository.test.ts are byte-for-byte unmodified by this phase, and the existing BusDataRepository test suite continues to pass unchanged (Phase 5 SC #4)."
  artifacts:
    - path: "src/server/api/repositories/FavoritesRecentsRepository.ts"
      provides: "Singleton SQLite-backed repository with initialize()/close() lifecycle, WAL + busy_timeout PRAGMAs, favorites/recents schema, and upsert/list CRUD for both entity types"
      exports: ["FavoritesRecentsRepository"]
    - path: "src/server/api/repositories/FavoritesRecentsRepository.test.ts"
      provides: "Lifecycle, CRUD, idempotency, and concurrency test coverage against a real temp-file SQLite database"
    - path: "src/server/api/models/PersistedEntity.ts"
      provides: "Shared EntityType/FavoriteRecord/RecentRecord types for the persistence layer"
      exports: ["EntityType", "FavoriteRecord", "RecentRecord"]
    - path: "src/server/config/environment.ts"
      provides: "DB_PATH env var (Zod-validated, defaulted) and environment.database.path (modified)"
    - path: "src/server/app.ts"
      provides: "FavoritesRecentsRepository initialize()/close() wired alongside BusDataRepository (modified)"
  key_links:
    - from: "src/server/app.ts"
      to: "src/server/api/repositories/FavoritesRecentsRepository.ts"
      via: "FavoritesRecentsRepository.getInstance().initialize() inside the startup Promise.all; .close() inside the shutdown handler"
      pattern: "favoritesRecentsRepository\\.(initialize|close)"
    - from: "src/server/api/repositories/FavoritesRecentsRepository.ts"
      to: "src/server/config/environment.ts"
      via: "new Database(environment.database.path) plus mkdirSync(dirname(environment.database.path))"
      pattern: "environment\\.database\\.path"
    - from: "src/server/api/repositories/FavoritesRecentsRepository.ts"
      to: "src/server/api/models/PersistedEntity.ts"
      via: "upsertFavorite/listFavorites/upsertRecent/listRecents parameter and return types"
      pattern: "EntityType|FavoriteRecord|RecentRecord"
  prohibitions:
    - "FavoritesRecentsRepository's read methods (listFavorites, listRecents) must never omit the device_id filter — a query without WHERE device_id = ? would leak one device's favorites/recents into another device's read (cross-device data isolation)."
---

<objective>
Stand up a tested, isolated SQLite-backed repository for favorites and recents — covering both routes and stops through one entity-typed `favorites` table and one entity-typed `recents` table (D-04) — with an explicit connection lifecycle wired into `app.ts` alongside (not inside) `BusDataRepository` (D-02, D-03), atomic UPSERT writes that satisfy the no-read-then-write-race concurrency requirement (D-05, D-07), and zero modification to `BusDataRepository`.

Purpose: Satisfies PERSIST-01 and all four Phase 5 ROADMAP success criteria; gives Phase 6 (Favorites) and Phase 7 (Recents) a complete, injectable repository to build services on, per the factory-function DI convention in CLAUDE.md.
Output: `FavoritesRecentsRepository.ts` (+ test suite), `PersistedEntity.ts` shared types, `DB_PATH`-aware `environment.ts`, and `app.ts` init/close wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-sqlite-persistence-foundation/05-CONTEXT.md
@.planning/phases/05-sqlite-persistence-foundation/05-PATTERNS.md
@src/server/api/repositories/BusDataRepository.ts
@src/server/api/repositories/BusDataRepository.test.ts
@src/server/api/repositories/index.ts
@src/server/config/environment.ts
@src/server/config/logger.ts
@src/server/api/models/index.ts
@src/server/api/models/Prediction.ts
@src/server/app.ts
</context>

## Artifacts This Phase Produces

New symbols and files introduced or modified by this plan, for downstream drift verification (Phase 6/7 will import these):

- **src/server/api/models/PersistedEntity.ts** (new file)
  - `export type EntityType = "route" | "stop";`
  - `export interface FavoriteRecord { deviceId: string; entityType: EntityType; entityId: string; favoritedAt: string; }`
  - `export interface RecentRecord { deviceId: string; entityType: EntityType; entityId: string; viewedAt: string; }`
- **src/server/api/models/index.ts** (modified) — new barrel line `export * from "./PersistedEntity";`
- **src/server/api/repositories/FavoritesRecentsRepository.ts** (new file)
  - `export class FavoritesRecentsRepository` with `private static instance`, `private constructor() {}`, `public static getInstance(): FavoritesRecentsRepository`
  - `public async initialize(): Promise<void>`, `public async close(): Promise<void>`
  - `public async upsertFavorite(deviceId: string, entityType: EntityType, entityId: string): Promise<void>`
  - `public async listFavorites(deviceId: string): Promise<FavoriteRecord[]>`
  - `public async upsertRecent(deviceId: string, entityType: EntityType, entityId: string): Promise<void>`
  - `public async listRecents(deviceId: string): Promise<RecentRecord[]>`
  - module-scoped constants `BUSY_TIMEOUT_MS`, `FAVORITES_TABLE_SQL`, `RECENTS_TABLE_SQL`
  - private helpers `assertInitialized(): void`, `handleError(action: "initialize" | "close", error: unknown): never`
- **src/server/api/repositories/FavoritesRecentsRepository.test.ts** (new file, test suite only)
- **src/server/api/repositories/index.ts** (modified) — new barrel line `export * from "./FavoritesRecentsRepository";`
- **src/server/config/environment.ts** (modified)
  - new schema field `DB_PATH: z.string().min(1).default("data/dash-tracker.sqlite")`
  - new exported `environment.database.path`
- **src/server/app.ts** (modified) — `favoritesRecentsRepository` initialized alongside `repository` via `Promise.all`; `.close()` called from the existing `shutdown` handler before `process.exit(0)`
- **.gitignore** (modified) — new `data/` ignore rule (D-02)
- **package.json** / **bun.lockb** (modified) — `better-sqlite3` and `@types/better-sqlite3` added to `dependencies`

## Prohibition Recall (PERSIST-01, prohibition-probe Stage 1/2)

Stage 1 (adversarial recall) surfaced ~8 candidates for "what could this feature silently become that the author would not want"; Stage 2 (precision) dropped routine-engineering items and kept/dismissed/canon-referred the following:

- **Kept (resolved/test)** — cross-device data isolation leakage: a bug omitting `WHERE device_id = ?` from `listFavorites`/`listRecents` would return one device's rows to another. Lifted into `must_haves.prohibitions` above and enforced by Task 2's device-isolation test.
- **Canon-referred** — SQL injection via `entity_id`/`device_id`/`entity_type` values is OWASP-canon, covered by `/gsd-secure-phase` + this plan's exclusive use of `better-sqlite3` prepared statements with bound parameters (never string concatenation); see threat T-05-01. Not minted as a bespoke prohibition.
- **Dismissed** — "device_id becomes a secondary tracking/fingerprinting mechanism": N/A for Phase 5's scope. `device_id` is stored as an opaque string column with no validation, parsing, or header-extraction logic in this phase — `X-Device-Id` header handling and validation is Phase 6's DEVICE-01, already explicitly out of scope for this phase per REQUIREMENTS.md.
- **Dismissed** — "synchronous driver blocks the event loop": already a locked, `costly`-rated tradeoff (D-01), not a silently-introduced risk; see Task 1's `<reversibility>` tag and threat T-05-04.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end SQLite connection lifecycle — env config, repository init/close, app.ts startup/shutdown wiring</name>
  <reversibility rating="costly">D-01 selects better-sqlite3 (synchronous API) as the SQLite driver; the user rated this "costly" during discuss-phase — swapping drivers later touches every repository method's call signature (sync internals wrapped in Promise<T> today) and the concurrency test's assertions in Task 3. The rating is carried forward from CONTEXT.md, not re-derived; the door is already walked through, no checkpoint is inserted for it.</reversibility>
  <precondition>Plan 05-01's package-legitimacy checkpoint was approved (better-sqlite3 and @types/better-sqlite3 are cleared to install).</precondition>
  <read_first>
    src/server/config/environment.ts (Zod environmentSchema + exported environment object — extend both, following the existing PORT field's z.coerce/.default(...) shape for the new DB_PATH field, per D-02); src/server/api/repositories/BusDataRepository.ts (singleton getInstance()/private-constructor shape, the handleLoadError message-extraction-and-rethrow idiom, the assertInitialized guard-before-access pattern — mirror all three structurally, but note this repository has no async fetch step so no initializationPromise lock is needed: better-sqlite3 calls are fully synchronous, so the whole initialize() body runs to completion before any other call can interleave, and the isInitialized guard alone prevents duplicate schema-creation); src/server/api/repositories/BusDataRepository.test.ts (the `@ts-expect-error` private-static-instance reset idiom in beforeEach/afterEach — reuse for FavoritesRecentsRepository's test); src/server/api/repositories/index.ts and src/server/api/models/index.ts (barrel `export * from "./X"` convention to extend); src/server/config/logger.ts (Winston instance — use logger.info on successful init/close, logger.error on failure, no per-write logging per CLAUDE.md); src/server/app.ts (current init/shutdown sequence: `const repository = BusDataRepository.getInstance(); repository.initialize().then(() => { ... process.on("SIGTERM"/"SIGINT", shutdown) }).catch(...)` — extend this, do not restructure it); .gitignore (current structure, to add a `data/` rule per D-02); package.json (current `dependencies` block shape — `@types/express`/`@types/winston` sit in `dependencies`, not `devDependencies`, in this project; match that placement for `@types/better-sqlite3`); .planning/phases/05-sqlite-persistence-foundation/05-CONTEXT.md decisions D-01 through D-04.
  </read_first>
  <files>package.json, bun.lockb, .gitignore, src/server/config/environment.ts, src/server/api/models/PersistedEntity.ts, src/server/api/models/index.ts, src/server/api/repositories/FavoritesRecentsRepository.ts, src/server/api/repositories/FavoritesRecentsRepository.test.ts, src/server/api/repositories/index.ts, src/server/app.ts</files>
  <action>
Run `bun add better-sqlite3 @types/better-sqlite3` — both land in `package.json`'s `dependencies` block (matching the existing `@types/express`/`@types/winston` placement, not `devDependencies`), updating `bun.lockb` as a side effect.

In src/server/config/environment.ts: add a `// biome-ignore lint/style/useNamingConvention: env vars use SCREAMING_SNAKE_CASE by convention` comment followed by `DB_PATH: z.string().min(1).default("data/dash-tracker.sqlite"),` to `environmentSchema`, directly below the existing `PORT` field (D-02). Add `database: { path: parsed.data.DB_PATH }` to the exported `environment` const object, as a new sibling to `dashApi` and `server`.

Create src/server/api/models/PersistedEntity.ts exporting exactly three named symbols: `export type EntityType = "route" | "stop";`, `export interface FavoriteRecord { deviceId: string; entityType: EntityType; entityId: string; favoritedAt: string; }`, `export interface RecentRecord { deviceId: string; entityType: EntityType; entityId: string; viewedAt: string; }`. `favoritedAt`/`viewedAt` are ISO 8601 strings, matching the `new Date().toISOString()` convention already used in `Prediction.ts`'s response types. Add `export * from "./PersistedEntity";` to src/server/api/models/index.ts, alongside the existing four barrel lines.

Create src/server/api/repositories/FavoritesRecentsRepository.ts. Import `Database` as the default export from `better-sqlite3` and derive the instance type via `type DatabaseInstance = InstanceType<typeof Database>;` (do not assume a named `Database` type export from `@types/better-sqlite3` — `InstanceType<typeof Database>` is always correct regardless of the exact typings shape). Import `mkdirSync` from `node:fs` and `dirname` from `node:path`. Import `environment`, `logger` from `../../config`. Import `EntityType`, `FavoriteRecord`, `RecentRecord` (type-only) from `../models`.

Define module-level constants: `const BUSY_TIMEOUT_MS = 5000;`, `const FAVORITES_TABLE_SQL = "CREATE TABLE IF NOT EXISTS favorites (id INTEGER PRIMARY KEY AUTOINCREMENT, device_id TEXT NOT NULL, entity_type TEXT NOT NULL CHECK (entity_type IN ('route', 'stop')), entity_id TEXT NOT NULL, favorited_at TEXT NOT NULL, UNIQUE (device_id, entity_type, entity_id))";`, `const RECENTS_TABLE_SQL = "CREATE TABLE IF NOT EXISTS recents (id INTEGER PRIMARY KEY AUTOINCREMENT, device_id TEXT NOT NULL, entity_type TEXT NOT NULL CHECK (entity_type IN ('route', 'stop')), entity_id TEXT NOT NULL, viewed_at TEXT NOT NULL, UNIQUE (device_id, entity_type, entity_id))";` (D-04: inline `CREATE TABLE IF NOT EXISTS` strings, no migration library; D-05: the composite `UNIQUE (device_id, entity_type, entity_id)` constraint on each table is what makes the atomic-UPSERT concurrency guarantee possible).

Export `class FavoritesRecentsRepository` with: `private db: DatabaseInstance | null = null;`, `private isInitialized = false;`, `private static instance: FavoritesRecentsRepository;`, `private constructor() {}`, `public static getInstance(): FavoritesRecentsRepository` (mirrors BusDataRepository's singleton exactly).

`public async initialize(): Promise<void>`: `if (this.isInitialized) return;` then inside a try/catch: `mkdirSync(dirname(environment.database.path), { recursive: true });` (better-sqlite3's `Database` constructor creates the DB file if missing but does NOT create missing parent directories, so this call is required before opening the connection or `data/dash-tracker.sqlite`'s default path fails on a fresh checkout); `this.db = new Database(environment.database.path);`; `this.db.pragma("journal_mode = WAL");`; `` this.db.pragma(`busy_timeout = ${BUSY_TIMEOUT_MS}`); ``; `this.db.exec(FAVORITES_TABLE_SQL);`; `this.db.exec(RECENTS_TABLE_SQL);`; `this.isInitialized = true;`; `` logger.info(`FavoritesRecentsRepository initialized at ${environment.database.path}`); ``. In the catch block, call `this.handleError("initialize", error)`.

`public async close(): Promise<void>`: `if (!this.db) return;` then inside a try/catch: `this.db.close();`; `this.db = null;`; `this.isInitialized = false;`; `logger.info("FavoritesRecentsRepository connection closed");`. In the catch block, call `this.handleError("close", error)`.

`private handleError(action: "initialize" | "close", error: unknown): never`: mirror BusDataRepository's `handleLoadError` idiom exactly — `const message = error instanceof Error ? error.message : "Unknown error";`, `` logger.error(`Failed to ${action} FavoritesRecentsRepository: ${message}`); ``, `` throw new Error(`Failed to ${action} FavoritesRecentsRepository: ${message}`); ``.

`private assertInitialized(): void`: `if (!this.isInitialized || !this.db) { throw new Error("FavoritesRecentsRepository has not been initialized. Call initialize() before accessing data."); }` (this task only needs the guard defined; the CRUD methods that call it are added in Task 2).

Add `export * from "./FavoritesRecentsRepository";` to src/server/api/repositories/index.ts, alongside the existing `BusDataRepository` line.

Modify src/server/app.ts: import `FavoritesRecentsRepository` from `./api/repositories` (add to the existing `BusDataRepository` import). After `const repository = BusDataRepository.getInstance();`, add `const favoritesRecentsRepository = FavoritesRecentsRepository.getInstance();`. Replace the `repository.initialize().then(...)` chain with `` Promise.all([repository.initialize(), favoritesRecentsRepository.initialize()]).then(() => { ... }) `` — keep the `.then()` body (server listen + shutdown wiring) and `.catch()` body (error-message extraction + `process.exit(1)`) otherwise unchanged, so both repositories must succeed before the server starts accepting requests, and either one failing produces the same existing failure path. Inside the `shutdown` closure, change `server.close(() => { logger.info(...); process.exit(0); });` to an async callback that awaits the new repository's teardown before exiting: `server.close(async () => { await favoritesRecentsRepository.close(); logger.info("Server is gracefully shutting down"); process.exit(0); });` (D-03: `close()` is wired into the existing SIGTERM/SIGINT handler, called from the shutdown closure, not a new signal handler).

Add a `data/` line to .gitignore (D-02 — the SQLite file directory is git-ignored, not committed). Place it under a short `### SQLite ###` comment near the existing tool-specific sections, not inside the `### Node ###` block (keeps the addition attributable to this phase in a diff).

Create src/server/api/repositories/FavoritesRecentsRepository.test.ts. Do NOT mock `better-sqlite3` (D-06 requires exercising real on-disk WAL/locking behavior, not a mock). Mock only `environment`/`logger` from `../../config` via `vi.mock("../../config", () => ({ environment: { database: { path: <per-test temp path> } }, logger: { info: vi.fn(), warn: vi.fn(), error: vi.fn() } }));` — but since the temp path must be unique per test run and computed inside the mock factory (vitest hoists `vi.mock` calls, so the factory cannot close over an outer `let` easily), instead compute `const TEST_DB_PATH = path.join(os.tmpdir(), \`dash-tracker-test-${randomUUID()}.sqlite\`);` as a top-level `const` above the `vi.mock` call (module-level consts ARE visible to hoisted `vi.mock` factories, unlike `let`/reassigned bindings), and reference `TEST_DB_PATH` directly inside the mock's `environment.database.path`. Import `path`/`os`/`node:crypto`'s `randomUUID`, `existsSync`/`rmSync` from `node:fs`. In `beforeEach`: `vi.clearAllMocks();`, reset the singleton via `// @ts-expect-error accessing private static for test isolation` + `FavoritesRecentsRepository.instance = undefined;`, then `repo = FavoritesRecentsRepository.getInstance();`. In `afterEach`: reset the singleton the same way, then remove `TEST_DB_PATH`, `` `${TEST_DB_PATH}-wal` ``, `` `${TEST_DB_PATH}-shm` `` with `rmSync(p, { force: true })` for each (WAL mode leaves sidecar files; `force: true` makes missing-file removal a no-op instead of throwing).

Write these test cases: (1) `getInstance()` always returns the same singleton instance (mirrors BusDataRepository.test.ts's equivalent test). (2) `initialize()` creates the underlying SQLite file at `TEST_DB_PATH` — assert `existsSync(TEST_DB_PATH)` is true after `await repo.initialize()`. (3) after `initialize()`, opening a second, independent `new Database(TEST_DB_PATH, { readonly: true })` connection and querying `` db2.prepare("SELECT name FROM sqlite_master WHERE type = 'table'").all() `` returns rows whose `name` values include both `"favorites"` and `"recents"` (WAL mode explicitly supports concurrent readers, so this second connection is safe to open while the repository's own connection stays open) — close `db2` at the end of this test. (4) after `initialize()`, the same second connection's `` db2.pragma("journal_mode", { simple: true }) `` returns `"wal"`. (5) `close()` does not throw when called without a prior `initialize()` call. (6) calling `initialize()` twice in sequence does not throw (idempotent guard).
  </action>
  <acceptance_criteria>
    - `bun run test -- src/server/api/repositories/FavoritesRecentsRepository.test.ts` exits 0 and includes all 6 test cases enumerated above.
    - `grep -c "favoritesRecentsRepository.initialize()" src/server/app.ts` returns 1.
    - `grep -c "favoritesRecentsRepository.close()" src/server/app.ts` returns 1, and that call sits inside the `shutdown` closure (visually confirm during review — not grep-checkable in isolation).
    - `grep -c "mkdirSync" src/server/api/repositories/FavoritesRecentsRepository.ts` returns 1 or more (parent-directory creation is present).
    - `bun run build` exits 0 (TypeScript compiles cleanly with the new dependency's types).
    - `git diff --stat -- src/server/api/repositories/BusDataRepository.ts src/server/api/repositories/BusDataRepository.test.ts` produces no output (SC #4 — untouched).
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- src/server/api/repositories/FavoritesRecentsRepository.test.ts && bun run build</automated>
  </verify>
  <done>Running the built server initializes both BusDataRepository and FavoritesRecentsRepository before accepting requests, and the SIGTERM/SIGINT shutdown handler closes the SQLite connection before the process exits — proven by the repository-level lifecycle test (file creation, both tables present in sqlite_master, WAL mode confirmed via a fresh raw connection) and the app.ts wiring grep checks, with BusDataRepository completely untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write/read methods for favorites and recents — both entity types, atomic upsert</name>
  <read_first>
    src/server/api/repositories/FavoritesRecentsRepository.ts (Task 1's committed implementation — read the actual file for the exact constructor/guard shape, not just this plan's prose); src/server/api/repositories/FavoritesRecentsRepository.test.ts (Task 1's test setup — TEST_DB_PATH, mock shape, beforeEach/afterEach — extend, do not rewrite); src/server/api/models/PersistedEntity.ts (EntityType/FavoriteRecord/RecentRecord — the exact field names these methods must satisfy); .planning/phases/05-sqlite-persistence-foundation/05-CONTEXT.md decision D-05 (composite UNIQUE constraint + single atomic INSERT ... ON CONFLICT DO UPDATE per write — no read-then-write).
  </read_first>
  <files>src/server/api/repositories/FavoritesRecentsRepository.ts, src/server/api/repositories/FavoritesRecentsRepository.test.ts</files>
  <behavior>
    - `upsertFavorite("device-a", "route", "route-1")` followed by `listFavorites("device-a")` returns exactly one `FavoriteRecord` with `entityType: "route"`, `entityId: "route-1"`.
    - `upsertFavorite("device-a", "stop", "stop-1")` then `upsertFavorite("device-a", "route", "route-1")` again followed by `listFavorites("device-a")` returns exactly two rows (one per entity type) — proves the single `favorites` table serves both entity types without a second parallel schema.
    - Calling `upsertFavorite("device-a", "route", "route-1")` twice in a row results in exactly one row for that `(deviceId, entityType, entityId)` in `listFavorites("device-a")`, with `favoritedAt` equal to the second call's timestamp, not the first (idempotency, D-05 — proves the ON CONFLICT DO UPDATE path, not a duplicate insert).
    - `listFavorites("device-b")` returns `[]` (empty array, not undefined) when `device-b` has never favorited anything, and never includes any row written for `"device-a"` (cross-device isolation — the kept prohibition).
    - `upsertRecent`/`listRecents` exhibit the identical write/read/idempotency/isolation behavior as `upsertFavorite`/`listFavorites`, against the separate `recents` table.
  </behavior>
  <action>
Add four public methods to `FavoritesRecentsRepository`, each starting with `this.assertInitialized();` then referencing `this.db as DatabaseInstance` for all subsequent driver calls in that method body (the guard throws before the cast is reached if the repository was never initialized, so the cast is safe at that point):

`public async upsertFavorite(deviceId: string, entityType: EntityType, entityId: string): Promise<void>`: `const favoritedAt = new Date().toISOString();` then `` (this.db as DatabaseInstance).prepare("INSERT INTO favorites (device_id, entity_type, entity_id, favorited_at) VALUES (@deviceId, @entityType, @entityId, @favoritedAt) ON CONFLICT (device_id, entity_type, entity_id) DO UPDATE SET favorited_at = @favoritedAt").run({ deviceId, entityType, entityId, favoritedAt }); `` (D-05: the `ON CONFLICT` clause targets the table's `UNIQUE (device_id, entity_type, entity_id)` constraint from Task 1's schema — this is the single atomic statement, no separate SELECT-then-INSERT/UPDATE).

`public async listFavorites(deviceId: string): Promise<FavoriteRecord[]>`: `` return (this.db as DatabaseInstance).prepare("SELECT device_id AS deviceId, entity_type AS entityType, entity_id AS entityId, favorited_at AS favoritedAt FROM favorites WHERE device_id = ? ORDER BY favorited_at DESC").all(deviceId) as FavoriteRecord[]; `` (the `AS` aliases map snake_case columns directly onto `FavoriteRecord`'s camelCase fields, so no manual row-mapping function is needed; the `WHERE device_id = ?` clause is the cross-device isolation boundary — never omit it).

`public async upsertRecent(deviceId: string, entityType: EntityType, entityId: string): Promise<void>`: identical shape to `upsertFavorite`, targeting the `recents` table and `viewed_at`/`viewedAt` instead of `favorited_at`/`favoritedAt`.

`public async listRecents(deviceId: string): Promise<RecentRecord[]>`: identical shape to `listFavorites`, targeting `recents`/`RecentRecord`/`viewedAt`, ordered `ORDER BY viewed_at DESC`.

Extend src/server/api/repositories/FavoritesRecentsRepository.test.ts with the five behaviors listed above as distinct `it` blocks (favorites write/read, both-entity-types-in-one-table, idempotent double-write, cross-device isolation, and the equivalent recents coverage) — each following the existing Arrange/Act/Assert comment style from BusDataRepository.test.ts.
  </action>
  <acceptance_criteria>
    - `bun run test -- src/server/api/repositories/FavoritesRecentsRepository.test.ts` exits 0 and includes at least the 5 behaviors enumerated above as distinct `it` blocks.
    - `grep -c "ON CONFLICT" src/server/api/repositories/FavoritesRecentsRepository.ts` returns 2 (one for `upsertFavorite`, one for `upsertRecent` — confirms no read-then-write path exists for either).
    - `grep -c "WHERE device_id = ?" src/server/api/repositories/FavoritesRecentsRepository.ts` returns 2 (one for `listFavorites`, one for `listRecents` — confirms the cross-device isolation prohibition is enforced on both read paths).
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- src/server/api/repositories/FavoritesRecentsRepository.test.ts</automated>
  </verify>
  <done>Favorite and recent rows can be written and read back for both entity types through upsertFavorite/listFavorites and upsertRecent/listRecents, each write is a single atomic UPSERT (idempotent, no duplicates), and every read is scoped to the requesting device.</done>
</task>

<task type="auto">
  <name>Task 3: Concurrency test (bump-to-top, no SQLITE_BUSY) and full-suite regression check</name>
  <read_first>
    src/server/api/repositories/FavoritesRecentsRepository.ts (Task 2's committed upsertRecent/listRecents implementation); src/server/api/repositories/FavoritesRecentsRepository.test.ts (current test file — extend, do not rewrite); src/server/api/repositories/BusDataRepository.test.ts (the file that must remain green and unmodified — read to confirm its current test count/shape as a baseline); .planning/phases/05-sqlite-persistence-foundation/05-CONTEXT.md decisions D-06 (real temp file, not :memory:) and D-07 (Promise.all through the public async API; this test proves the UPSERT logic is correct under near-simultaneous calls on a single Node event loop, not true OS-level thread concurrency — matches how request handlers will actually call this repository).
  </read_first>
  <files>src/server/api/repositories/FavoritesRecentsRepository.test.ts</files>
  <action>
Add a new `describe("concurrency", ...)` block to src/server/api/repositories/FavoritesRecentsRepository.test.ts (after `await repo.initialize()` in a fresh `beforeEach`/inline arrange, per the existing setup). Test 1 — "handles concurrent bump-to-top writes to the same device+entity without throwing or duplicating rows": Arrange a `const beforeBurst = new Date().toISOString();` marker, then `Promise.all(Array.from({ length: 25 }, () => repo.upsertRecent("device-a", "stop", "stop-1")))`; Act by awaiting that `Promise.all` inside an `expect(...).resolves.not.toThrow()` (or a plain `await` with no surrounding try/catch — either proves no rejection propagates); Assert: `` const rows = await repo.listRecents("device-a"); `` — `rows` has length 1, `rows[0].entityId` is `"stop-1"`, `rows[0].entityType` is `"stop"`, and `rows[0].viewedAt >= beforeBurst` (string comparison is valid for ISO 8601 timestamps) — proving the surviving row's timestamp was captured during the burst, not stale from before it. Also assert the resolved `Promise.all` array has length 25 (all 25 calls settled, none hung or threw). Test 2 — "isolates concurrent writes across two different devices for the same entity": `Promise.all([...12 calls to upsertRecent("device-a", "route", "route-9"), ...12 calls to upsertRecent("device-b", "route", "route-9")])`; Assert `(await repo.listRecents("device-a"))` has exactly 1 row for `route-9` and `(await repo.listRecents("device-b"))` has exactly 1 row for `route-9` — two independent rows total (one per device), never merged or cross-contaminated, extending the cross-device isolation prohibition to the concurrent-write path (D-07).

After adding these tests, run the full project test suite once to confirm SC #4: `src/server/api/repositories/BusDataRepository.test.ts` was never touched by any task in this plan and its existing test count/assertions are unaffected.
  </action>
  <acceptance_criteria>
    - `bun run test -- src/server/api/repositories/FavoritesRecentsRepository.test.ts` exits 0, includes a `describe("concurrency", ...)` block, and neither concurrency test throws or contains a caught/swallowed `SQLITE_BUSY` string anywhere in its assertions (search the test file: `grep -c "SQLITE_BUSY" src/server/api/repositories/FavoritesRecentsRepository.test.ts` returns 0 — the point is that this error class never occurs, so the test never needs to reference it).
    - `bun run test` (full suite, project-wide, with `--typecheck`) exits 0.
    - `git diff --stat -- src/server/api/repositories/BusDataRepository.ts src/server/api/repositories/BusDataRepository.test.ts` produces no output.
    - `bun run test:coverage` still meets the 80% branches/functions/lines/statements thresholds project-wide.
  </acceptance_criteria>
  <verify>
    <automated>bun run test</automated>
  </verify>
  <done>25 concurrent bump-to-top writes to the same device+entity resolve without any exception and leave exactly one row behind; concurrent writes from two different devices to the same entityId never cross-contaminate; the full project test suite (including BusDataRepository's untouched suite) and coverage thresholds pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| App process -> local SQLite file (`environment.database.path`) | The server process is the sole writer/reader; Phase 5 has no HTTP surface yet, so the boundary here is process -> filesystem, not client -> server |
| Future callers (Phase 6/7 services) -> FavoritesRecentsRepository public methods | `deviceId`/`entityType`/`entityId` are not yet sourced from an untrusted HTTP header in this phase, but are treated as untrusted at the repository boundary now, since Phase 6 will pass an unvalidated `X-Device-Id` header value straight through |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering / Injection | FavoritesRecentsRepository SQL construction (upsertFavorite/listFavorites/upsertRecent/listRecents) | high | mitigate | Every statement uses `better-sqlite3` prepared statements with named (`@deviceId` etc.) or positional (`?`) bound parameters — never string concatenation or template-literal SQL built from caller-supplied values. SQL injection is OWASP-canon, covered by this parameterization + `/gsd-secure-phase`; not minted as a bespoke prohibition (prohibition-recall canon-referral). |
| T-05-02 | Information Disclosure | listFavorites/listRecents cross-device leakage | high | mitigate | Every read filters by `WHERE device_id = ?` (Task 2); Task 2's and Task 3's tests assert one device's list never contains another device's rows, including under concurrent writes. Lifted into `must_haves.prohibitions`. |
| T-05-03 | Tampering | `DB_PATH` env var (file-path handling) | medium | mitigate | `DB_PATH` is Zod-validated (`z.string().min(1)`) and defaults to a fixed, repo-relative `data/dash-tracker.sqlite`; it is operator-supplied config (not client input, matching the existing `PORT` pattern) — no path-traversal surface from HTTP requests since Phase 5 has no HTTP endpoints. |
| T-05-04 | Denial of Service | Synchronous better-sqlite3 calls blocking the Node event loop | low | accept | D-01 (locked, rated `costly`) already accepts this tradeoff — write payloads are a handful of scalar columns, so per-call blocking time is sub-millisecond; a deliberate, already-reviewed driver choice, not a new risk introduced by this plan. |
| T-05-05 | Repudiation | No audit trail beyond the row itself | low | accept | Out of scope for Phase 5 (persistence plumbing only); `favorited_at`/`viewed_at` timestamps provide the minimal trail this feature needs. |
</threat_model>

<verification>
- `bun run test` passes (full suite, with `--typecheck`) after all three tasks.
- `bun run test:coverage` meets the 80% branches/functions/lines/statements thresholds.
- `bun run build` compiles cleanly.
- `bun run lint` exits 0 for new/modified files.
- `git diff --stat -- src/server/api/repositories/BusDataRepository.ts src/server/api/repositories/BusDataRepository.test.ts` is empty (SC #4).
- Manual smoke check (optional, not required for automated pass): `bun run dev-server`, confirm `data/dash-tracker.sqlite` is created on startup and the process exits cleanly on Ctrl+C.
</verification>

<success_criteria>
- Phase 5 SC #1: Server starts up and shuts down cleanly with the new SQLite connection lifecycle wired into `app.ts`'s init/shutdown sequence, alongside BusDataRepository.
- Phase 5 SC #2: Favorite and recent rows can be written and read back for both entity types (route and stop) through the repository's storage methods, via one entity-typed table per concern.
- Phase 5 SC #3: Concurrent bump-to-top writes to the same device's recents complete via a single atomic UPSERT and never throw SQLITE_BUSY, verified under a concurrency test.
- Phase 5 SC #4: BusDataRepository and its existing tests are completely unmodified and continue to pass unchanged.
- PERSIST-01 fully satisfied: favorites/recents persist in a new, isolated SQLite-backed repository following the routes -> controllers -> services -> repository architecture (repository layer only — Phase 6/7 add the remaining layers).
</success_criteria>

<output>
Create `.planning/phases/05-sqlite-persistence-foundation/05-02-SUMMARY.md` when done
</output>
