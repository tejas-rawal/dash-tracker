---
phase: 05-service-alerts-ingestion
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/api/services/ServiceAlertService.ts
  - src/server/api/services/ServiceAlertService.test.ts
  - src/server/api/models/ServiceAlert.ts
autonomous: false
requirements: [ALRT-01, ALRT-03]
gap_closure: true
gap_ids: [G-05-5]

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "fetchFromDashApi() accepts a top-level JSON array body (or a JSON-encoded string that parses to one) as the entities list directly, with no {entities:[...]} envelope required, while still rejecting null and non-object/non-array primitive bodies as malformed."
    - "The real top-level shape of the live gtfs-rt-alerts/v2?format=json response — and the header_text/description_text nested shape of a real entity — is confirmed via a human-reported live-boot diagnostic capture, not re-guessed from an unverified example (the mistake that produced G-05-2's original envelope assumption)."
    - "fetchAlerts() resolves successfully end-to-end against the confirmed live shape, with headerText/descriptionText/url populated from real feed text (not undefined) — closing both UAT Test 1 (poll succeeds) and Test 2 (field-shape check)."
    - "The two existing tests that currently assert an array-rooted body is malformed are corrected to match the new accepted behavior, so the test suite documents reality instead of the disproven assumption."
  artifacts:
    - src/server/api/services/ServiceAlertService.ts (modified — guard + normalization fix, temporary diagnostic log added then removed)
    - src/server/api/services/ServiceAlertService.test.ts (modified — corrected WR-01 array tests, new no-envelope regression tests, live-shape fixture test)
    - src/server/api/models/ServiceAlert.ts (modified only if the confirmed header_text/description_text shape differs from the current single-object type)
  key_links:
    - "fetchFromDashApi() normalizes whatever top-level shape is confirmed (bare array, or an object with an entities-like key) into the same { entities: DashAlertEntity[] | undefined } contract fetchAlerts() already consumes unchanged."
    - "The checkpoint:human-action task's reported diagnostic output is the sole source of truth Task 3 codes against — no shape is assumed without it, breaking the G-05-2 -> G-05-5 pattern of shipping unverified live-shape guesses."
---

<objective>
Close UAT gap G-05-5: after the 05-05 `format=json` fix, live boot still logs `Failed to poll service alerts: DASH API returned a malformed service alerts response (body is not an object)` because `fetchFromDashApi()`'s guard rejects any array-rooted (or otherwise non-plain-object) top-level JSON value — and the debug session (`.planning/debug/service-alerts-body-shape.md`) concluded the live response's actual top-level shape was never genuinely confirmed, only inferred from an unreliable, likely auto-generated example in G-05-2.

This plan captures the real shape via a temporary diagnostic log plus one human-action checkpoint (booting the live server, which only the human can reach), then implements the permanent fix against that confirmed evidence instead of guessing again.

Purpose: ALRT-01 (alerts must actually fetch from the live endpoint without erroring) and ALRT-03 (each alert must be normalized with real header/description/url text, not undefined) are both blocked until the poll succeeds end-to-end against the real payload shape.

Output: `fetchFromDashApi()`/`fetchAlerts()` handle the confirmed real top-level shape (most likely a bare top-level array of entities, per the debug session's evidence-based analysis, but confirmed rather than assumed); `ServiceAlert.ts`'s header_text/description_text typing corrected if the live sample disagrees with it; test suite updated to match confirmed reality, including a live-shape-derived regression fixture.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-service-alerts-ingestion/08-UAT.md
@.planning/debug/service-alerts-body-shape.md
@src/server/api/services/ServiceAlertService.ts
@src/server/api/models/ServiceAlert.ts
@src/server/api/services/ServiceAlertService.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a temporary diagnostic capture of the real response shape</name>
  <files>src/server/api/services/ServiceAlertService.ts</files>
  <read_first>src/server/api/services/ServiceAlertService.ts, .planning/debug/service-alerts-body-shape.md</read_first>
  <action>
    In `fetchFromDashApi()`, immediately after the existing string/`JSON.parse` fallback block (i.e. once `body` holds whatever value axios's own transform plus the app's own fallback produced) and strictly BEFORE the `if (body === null || typeof body !== "object" || Array.isArray(body))` guard, add exactly one diagnostic line: `logger.warn` a message that includes a distinctive tag `SERVICE-ALERTS-SHAPE-DIAG`, `typeof body`, and a truncated (first 2000 characters) `JSON.stringify(body)`. Place a one-line comment directly above it stating this is a TEMPORARY diagnostic added to confirm the real live top-level shape before implementing the permanent fix, and that Task 3 of this same plan removes it once the shape is confirmed.

    Do not touch the guard's existing rejection condition or any other logic in this task — leave the current behavior (including throwing on array-rooted bodies) exactly as-is. This task only adds visibility into what the guard is about to reject or accept; the fix itself is Task 3, gated on the human-reported evidence from Task 2.
  </action>
  <verify>
    <automated>bun run test -- src/server/api/services/ServiceAlertService.test.ts</automated>
    <fails_when>non-zero exit code, or the vitest summary output contains "failed"</fails_when>
  </verify>
  <done>The diagnostic log line is present immediately before the malformed-body guard in fetchFromDashApi(), and every pre-existing ServiceAlertService test still passes unmodified (no behavior change yet, only added visibility).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Capture the real live response shape via a human-run boot</name>
  <action>Boot the live server against the real DASH/Swiftly API and report back the exact diagnostic line, so the permanent fix can be coded against confirmed reality instead of another guess.</action>
  <instructions>
    Task 1 added a temporary `SERVICE-ALERTS-SHAPE-DIAG` diagnostic log to `fetchFromDashApi()`. I cannot trigger this myself — every prior round of this gap (G-05-1 through G-05-4) was only reproducible against the live DASH/Swiftly API from your machine, using your configured `DASH_API_KEY` and network access.

    1. Run `bun run dev-server` (or `bun run start-server`) locally against the live API.
    2. The service-alerts poll fires immediately at boot — no waiting needed. Watch the console output.
    3. Copy the full `SERVICE-ALERTS-SHAPE-DIAG` line (the `typeof body=...` and truncated `body=...` JSON snippet). If the snippet includes at least one full alert entity, that's ideal — it also lets us confirm the header_text/description_text shape in the same step.
    4. If instead you see a DIFFERENT error before that diagnostic line ever logs (e.g. a crash upstream of the guard), paste that instead — it means something changed beyond what this plan anticipated and needs fresh diagnosis rather than a guess.
    5. Paste whatever you captured back here.
  </instructions>
  <verification>Once you paste the line, I will parse `typeof body` and the JSON snippet to confirm the exact top-level shape (bare array vs. an object with an entities-like key vs. something else) and, if a full entity is present, whether header_text/description_text/url are single objects or arrays of objects — then implement Task 3 against that confirmed evidence.</verification>
  <resume-signal>Paste the full SERVICE-ALERTS-SHAPE-DIAG console line (or the alternate error, if the diagnostic line never logged)</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the confirmed-shape fix, remove the diagnostic, correct the tests</name>
  <files>src/server/api/services/ServiceAlertService.ts, src/server/api/models/ServiceAlert.ts, src/server/api/services/ServiceAlertService.test.ts</files>
  <read_first>src/server/api/services/ServiceAlertService.ts, src/server/api/models/ServiceAlert.ts, src/server/api/services/ServiceAlertService.test.ts, .planning/debug/service-alerts-body-shape.md</read_first>
  <behavior>
    - A bare top-level JSON array response body (object form, e.g. `data: [entity]`, and JSON-string form, e.g. `data: "[]"`) is now accepted and mapped instead of rejected — including the empty-array case resolving to `[]`.
    - `null` bodies and non-object/non-array primitive bodies (e.g. a string that fails re-parsing, `"not-an-object"`) still reject with `UpstreamApiError` — unchanged, regression-proofed.
    - The pre-existing `{ entities: [...] }` object-envelope shape, undefined-entities warning path, non-array-entities rejection, and per-entity validation (WR-02) all continue to work exactly as before.
    - A new test built from the confirmed live evidence (Task 2's report) proves `fetchAlerts()` resolves end-to-end with `headerText`/`descriptionText`/`url` populated from real feed text, not undefined.
  </behavior>
  <action>
    Using the exact shape reported by the human in Task 2, implement the permanent fix:

    1. In `fetchFromDashApi()`'s guard, stop rejecting arrays: change the rejection condition from checking null-or-non-object-or-array to checking only null-or-non-object (an array's `typeof` is `"object"` in JavaScript, so removing the array check is sufficient — `null` and non-object primitives like strings/numbers/booleans remain rejected exactly as before).

    2. Immediately after that guard, normalize whichever shape Task 2 confirmed into the same `{ entities: DashAlertEntity[] | undefined }` contract `fetchAlerts()` already consumes, so `fetchAlerts()` itself needs no changes: if the body is an array, treat the array itself as the entities list; otherwise read the entities list off the object using whichever key the confirmed live payload actually used (most likely `entities`, matching the current `DashAlertsApiResponse` type — adjust the key read if Task 2's report shows a different key name, e.g. the GTFS-RT-canonical singular `entity`). If Task 2's report instead shows the guard is still being hit unexpectedly (body genuinely null or a non-re-parsing string), stop and flag this for fresh diagnosis rather than guessing further — do not repeat the G-05-2/G-05-5 pattern of shipping an unverified assumption.

    3. Re-check the `header_text`/`description_text`/`url` shape against the sample entity in Task 2's captured output, if one is present. If it shows a single `{translation: [...]}` object per field — matching the current `DashAlertTranslatedString` type and the current `alert.header_text?.translation?.[0]?.text` accessor pattern in `mapToServiceAlert()` — leave `ServiceAlert.ts` and the accessors unchanged, and note in the SUMMARY that this was verified against live evidence, not assumed. If it instead shows an array of `{translation: [...]}` objects per field, update `DashAlert.header_text`/`description_text`/`url` in `ServiceAlert.ts` to an array of `DashAlertTranslatedString`, and update the three corresponding accessors in `mapToServiceAlert()` to index into that array before reading `.translation?.[0]?.text`.

    4. Remove the temporary diagnostic log line and its comment added in Task 1 — the shape is now confirmed and permanently handled by code, not by runtime discovery.

    5. Update `ServiceAlertService.test.ts`: change the test currently asserting an array-rooted response body is rejected to instead assert it resolves to an empty array (rename it to reflect the new accepted behavior); change the equivalent test for a JSON-encoded string that parses to an array the same way; add a new test asserting a non-empty bare array-rooted body (no envelope) maps correctly end-to-end (id, informedRouteIds, informedStopIds match); add one test built from the field values reported in Task 2's live capture asserting `fetchAlerts()` resolves with real `headerText`/`descriptionText`/`url` populated (not undefined) — if the captured snippet didn't include a complete entity, build this fixture from the confirmed top-level shape combined with the existing realistic field values already used elsewhere in the file, and note in the SUMMARY that the field-level content is shape-accurate rather than a literal live capture. Leave every other existing test (object-envelope success cases, null-body rejection, non-object-primitive rejection, per-entity WR-02 validation) unchanged and passing.
  </action>
  <acceptance_criteria>
    - `fetchFromDashApi()`'s guard no longer rejects an array-rooted body; it still rejects `null` and any non-object primitive.
    - A bare top-level array response (both raw-object and JSON-string-encoded forms) resolves successfully via `fetchAlerts()` instead of throwing `UpstreamApiError`.
    - `null` and non-object/non-array primitive bodies still throw `UpstreamApiError`, proven by the still-passing pre-existing tests for those cases.
    - The temporary diagnostic log line and its comment from Task 1 are fully gone from `ServiceAlertService.ts`.
    - At least one test asserts `headerText`/`descriptionText`/`url` all populate correctly from a fixture reflecting the confirmed live shape.
    - `bun run build` succeeds and the full workspace test suite (`bun run test`) passes with no coverage regression below 80%.
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- src/server/api/services/ServiceAlertService.test.ts && bun run build</automated>
    <fails_when>non-zero exit code from either command, or the vitest summary output contains "failed"</fails_when>
    <human-check>Boot the real server (bun run dev-server or start-server) against the live DASH/Swiftly API. Confirm: (1) no "Failed to poll service alerts: ..." error is logged at all; (2) the SERVICE-ALERTS-SHAPE-DIAG line is gone (confirms cleanup); (3) with at least one active alert, the fetched alert's headerText/descriptionText/url reflect real feed text, not undefined.</human-check>
  </verify>
  <reversibility rating="reversible">In-memory-only parsing/normalization logic with no external contract or persisted state — a same-file edit if the confirmed shape needs further correction later.</reversibility>
  <done>fetchAlerts() succeeds end-to-end against the confirmed live shape, the temporary diagnostic is removed, headerText/descriptionText/url map correctly, and the test suite documents the new, confirmed behavior — closing UAT G-05-5 (Test 1) and its blocked Test 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| DASH/Swiftly service-alerts API -> `ServiceAlertService` | Untrusted external upstream response (network-controlled JSON, top-level shape now confirmed via live evidence rather than assumed) crosses into the app's fetch/parse/mapping layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-12 | Tampering | `ServiceAlertService.fetchFromDashApi()` guard normalization | high | mitigate | Widening the guard to accept array-rooted bodies must not accidentally accept genuinely malformed input — regression tests explicitly lock in that `null` and non-object/non-array primitive bodies still throw `UpstreamApiError` after the guard change, and the accepted shape itself is confirmed via a human-reported live capture (Task 2) rather than assumed, closing the defect class that produced G-05-2 and G-05-5. |
| T-05-13 | Information Disclosure | Temporary `SERVICE-ALERTS-SHAPE-DIAG` diagnostic log (Task 1) | low | mitigate | Scoped to non-sensitive public transit alert data (no PII/credentials appear in the GTFS-RT alerts payload), truncated to 2000 characters, and removed by Task 3 within this same plan before the plan is considered complete — never ships to a long-lived state. |
</threat_model>

<verification>
- `bun run test -- src/server/api/services/ServiceAlertService.test.ts` passes with zero failures after Task 3.
- `bun run test` (full workspace suite) passes with zero failures and no coverage regression below the 80% threshold.
- `bun run build` succeeds.
- Live re-verification (human-check on Task 3): booting the real server against the live DASH/Swiftly API produces no "Failed to poll service alerts" error, and real alert headerText/descriptionText/url populate.
</verification>

<success_criteria>
- `fetchFromDashApi()`/`fetchAlerts()` handle the actual, confirmed top-level shape of the live `gtfs-rt-alerts/v2?format=json` response — not a guessed shape.
- The poll succeeds end-to-end against the live API with no malformed-body error (closes UAT G-05-5 Test 1).
- Real alert `headerText`/`descriptionText`/`url` populate from feed text once entities are reachable (closes UAT G-05-5's blocked Test 2).
- The temporary diagnostic capture is fully removed; only the permanent, confirmed-shape-aware code remains.
</success_criteria>

<output>
Create `.planning/phases/08-service-alerts-ingestion/08-06-SUMMARY.md` when done
</output>
