---
phase: 05-sqlite-persistence-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [PERSIST-01]

estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "A human has verified `better-sqlite3` and `@types/better-sqlite3` on npmjs.com before either package is installed, since this project has research disabled (no RESEARCH.md `## Package Legitimacy Audit` exists) and the Package Legitimacy Gate's fallback `[ASSUMED]` policy applies to D-01's driver choice."
  artifacts: []
  key_links: []
---

<objective>
Gate the `better-sqlite3` dependency install behind a human-verified package-legitimacy check before Plan 05-02 runs `bun add`. Research is disabled for this project (`workflow.research=false`), so no RESEARCH.md `## Package Legitimacy Audit` exists to pre-clear the package — the Package Legitimacy Gate's fallback policy treats `better-sqlite3` and `@types/better-sqlite3` as `[ASSUMED]` and requires a `gate="blocking-human"` checkpoint (never auto-approved, even in yolo/auto-chain mode) before either package touches `package.json`.

Purpose: Prevent an unattended run from installing an unvetted native-binding npm package as part of implementing PERSIST-01 (D-01, CONTEXT.md).
Output: Human confirmation to proceed, recorded via the checkpoint resume signal. No files are created or modified by this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-sqlite-persistence-foundation/05-CONTEXT.md
</context>

## Artifacts This Phase Produces

No new artifacts — this plan is a human verification gate only. It creates and modifies no files.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Verify better-sqlite3 package legitimacy before install</name>
  <what-built>
    D-01 (locked decision, `.planning/phases/05-sqlite-persistence-foundation/05-CONTEXT.md`) selects `better-sqlite3` — a synchronous, native-binding SQLite driver — as a new production dependency for the Favorites/Recents persistence layer (PERSIST-01), alongside its type definitions `@types/better-sqlite3`. This project has research disabled (`workflow.research=false`), so no RESEARCH.md `## Package Legitimacy Audit` table exists for either package. Per the Package Legitimacy Gate's fallback policy, both packages are treated as `[ASSUMED]` and require a human-verified check before Plan 05-02 runs `bun add better-sqlite3 @types/better-sqlite3`.
  </what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/better-sqlite3 and confirm: weekly download count is in the millions (mainstream, widely used), the "Repository" link resolves to github.com/WiseLibs/better-sqlite3 with real, ongoing commit/release history, and the latest published version is recent (not abandoned).
    2. Visit https://www.npmjs.com/package/@types/better-sqlite3 and confirm it resolves to the DefinitelyTyped-maintained types package (repository under github.com/DefinitelyTyped/DefinitelyTyped) for the same library — not an unrelated or typosquatted package.
    3. Confirm neither package name is a typosquat of a more popular package (e.g. not `better-sqlite` or `better_sqlite3`).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with `bun add better-sqlite3 @types/better-sqlite3` in Plan 05-02, or describe concerns to halt.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Developer machine -> npm registry | `bun add` fetches and runs install-time build/download logic (better-sqlite3 compiles or downloads a prebuilt native binary) sourced from an external registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | `bun add better-sqlite3 @types/better-sqlite3` (supply-chain / native install script) | high | mitigate | Package Legitimacy Gate: this plan's `gate="blocking-human"` checkpoint requires a human to verify both packages on npmjs.com (maintainer, download count, repository, no typosquat) before Plan 05-02 runs the install. Never auto-approved by `workflow.auto_advance`/auto-chain mode. |
</threat_model>

<verification>
The checkpoint resolves with an explicit "approved" resume signal from the user before Plan 05-02 begins. No automated check applies — this plan's sole output is human sign-off.
</verification>

<success_criteria>
`better-sqlite3` and `@types/better-sqlite3` are confirmed legitimate by a human before any install command runs, closing the Package Legitimacy Gate's `[ASSUMED]`-fallback requirement for D-01.
</success_criteria>

<output>
Create `.planning/phases/05-sqlite-persistence-foundation/05-01-SUMMARY.md` when done
</output>
