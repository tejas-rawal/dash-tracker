---
phase: 06-alerts-surfaced-on-routes-stops-predictions
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/server/api/repositories/ServiceAlertRepository.ts
  - src/server/api/repositories/ServiceAlertRepository.test.ts
  - src/server/api/models/StopDiscovery.ts
  - src/server/api/services/StopService.ts
  - src/server/api/services/StopService.test.ts
  - src/server/api/routes/busRoutes.ts
  - src/server/api/routes/busRoutes.test.ts
  - src/server/api/routes/stopRoutes.ts
  - src/server/api/routes/stopRoutes.test.ts
autonomous: true
requirements: [ALRT-07, ALRT-08]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "GET /api/v1/routes/:shortName/stops: every stop in every direction's stops array includes an alerts field."
    - "GET /api/v1/stops/nearby: every returned stop includes an alerts field."
    - "A stop's alerts array contains a trimmed ServiceAlertSummary for every currently-active ServiceAlert whose informedStopIds includes that stop's id (D-03)."
    - "A stop with zero currently-active matching alerts has alerts: [] — never omitted, never null/undefined (D-02)."
    - "An alert with both informedRouteIds and informedStopIds empty (agency-wide) is never returned by ServiceAlertRepository.getActiveAlertsForStop, so it never appears in any stop's alerts array (D-04)."
    - "Distance-ascending sort order in GET /stops/nearby (existing behavior) is unaffected by alert embedding — alerts are attached without altering the sort key."
    - "A stop present in multiple directions of the same route gets its own independently-computed alerts array in each direction's stop entry — no cross-direction merging."
    - "Two nearby stops informed by the same route-wide or stop-specific alert each independently include it in their own alerts array — stops are never merged or deduped because they share an alert."
    - statement: "Concurrent ServiceAlertPollService.applyAlerts() swaps never produce a torn/partial read during a stop-alerts lookup (same atomic full-map-replacement guarantee getActiveAlerts() already relies on)."
      verification: backstop
  artifacts: []
  key_links:
    - "StopService.getStopsForRoute/getNearbyStops -> ServiceAlertRepository.getActiveAlertsForStop(stop.id) -> mapToServiceAlertSummaries -> StopWithAlerts.alerts / NearbyStop.alerts"
    - "src/server/api/routes/busRoutes.ts's stopService instantiation and src/server/api/routes/stopRoutes.ts both wire ServiceAlertRepository.getInstance() into createStopService as its second constructor argument"
  prohibitions:
    - "No changes to the SSE prediction stream or GET /api/v1/predictions response shape (out of scope per D-05)."
    - "The embedded alert summary object never includes url, informedRouteIds, or informedStopIds fields (D-01)."
    - "The alerts field is never omitted from a stop response, even when empty (D-02)."
    - "StopService never imports ServiceAlertRepository.getInstance() directly — it only receives the repository via its factory-function parameter."
    - "Stop-to-alert matching never uses code (the numeric display code) in place of id (D-03)."
    - "An alert with both informedRouteIds and informedStopIds empty is never attached to a stop (D-04)."
---

<objective>
Expand the alert-embedding pattern proven in Plan 06-01 to stop responses: `GET /api/v1/routes/:shortName/stops` and `GET /api/v1/stops/nearby` (ALRT-07, ALRT-08). Reuses `ServiceAlertSummary`/`StopWithAlerts` (from `models/ServiceAlertSummary.ts`) and `mapToServiceAlertSummaries` (from `services/serviceAlertMapping.ts`), both created in Plan 06-01 — this plan only adds the stop-side repository query, `StopService` embedding, and DI wiring.

Purpose: Complete ROADMAP Phase 6's goal — every route and stop response a rider can request now carries its currently-active alerts, with no new endpoint and no SSE changes (D-05).
Output: `StopWithAlerts`-shaped stop entries in `StopService.getStopsForRoute()`'s grouped-by-direction response, and alert-carrying entries from `StopService.getNearbyStops()`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-alerts-surfaced-on-routes-stops-predictions/06-CONTEXT.md
@src/server/api/repositories/ServiceAlertRepository.ts
@src/server/api/models/StopDiscovery.ts
@src/server/api/services/StopService.ts
@src/server/api/services/serviceAlertMapping.ts
@src/server/api/models/ServiceAlertSummary.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand alert embedding to stop responses — routes/:shortName/stops &amp; stops/nearby</name>
  <files>src/server/api/repositories/ServiceAlertRepository.test.ts, src/server/api/models/StopDiscovery.ts, src/server/api/services/StopService.ts, src/server/api/services/StopService.test.ts, src/server/api/routes/busRoutes.ts, src/server/api/routes/stopRoutes.ts</files>
  <read_first>
    src/server/api/repositories/ServiceAlertRepository.ts (getActiveAlertsForStop already added by Plan 06-01 — verify it exists before proceeding; do not re-add it)
    src/server/api/repositories/ServiceAlertRepository.test.ts (makeAlert() factory, and the getActiveAlertsForRoute describe block added by Plan 06-01 as the pattern to mirror for getActiveAlertsForStop)
    src/server/api/models/StopDiscovery.ts (RouteDirectionStops/NearbyStop — the two response-shape interfaces to extend)
    src/server/api/models/ServiceAlertSummary.ts (StopWithAlerts — already defined by Plan 06-01; import it, do not redefine it)
    src/server/api/services/serviceAlertMapping.ts (mapToServiceAlertSummaries — reuse, do not duplicate)
    src/server/api/services/StopService.ts (factory-DI + getStopsForRoute/getNearbyStops shapes to extend)
    src/server/api/services/StopService.test.ts (makeMockRepo()/makeRoute()/makeStop()/makeStopAt() helpers and every existing assertion that touches a stop object shape)
    src/server/api/routes/busRoutes.ts (the stopService instantiation line, distinct from the busRouteService line Plan 06-01 already updated)
    src/server/api/routes/stopRoutes.ts (the DI wiring call site for createStopService)
    .planning/phases/06-alerts-surfaced-on-routes-stops-predictions/06-CONTEXT.md (D-01 through D-04)
  </read_first>
  <behavior>
    - ServiceAlertRepository.getActiveAlertsForStop(stopId, referenceTime = new Date()) — confirm (from Plan 06-01) it filters getActiveAlerts() by alert.informedStopIds.includes(stopId); add the full ServiceAlertRepository.test.ts coverage for it now (Plan 06-01 only implemented the method, it did not add its tests): matching stop, non-matching stop, expired-but-matching, agency-wide exclusion (D-04), and multi-alert stable-order.
    - StopService.getStopsForRoute(shortName): every stop object in every direction's stops array gains alerts: ServiceAlertSummary[] via getActiveAlertsForStop(stop.id), mapped through mapToServiceAlertSummaries; [] when no match; a stop appearing in 2 directions gets getActiveAlertsForStop called once per occurrence (independently computed per direction, not shared/cached).
    - StopService.getNearbyStops(lat, lng, options): every stop object in the returned array gains the same alerts field, computed for each stop before the existing distance/radius-filter/sort/slice pipeline runs; sort order (ascending by distance) is unaffected since alerts is never used as a sort key.
    - createStopService(repository, serviceAlertRepository) requires both arguments — the service never imports ServiceAlertRepository.getInstance() directly.
  </behavior>
  <action>
    First, confirm `ServiceAlertRepository.getActiveAlertsForStop` already exists (added in Plan 06-01's Task 1) — if it is missing, add it now as `getActiveAlertsForStop(stopId: string, referenceTime: Date = new Date()): ServiceAlert[]` returning `this.getActiveAlerts(referenceTime).filter((alert) => alert.informedStopIds.includes(stopId))`. Then add a `describe("getActiveAlertsForStop")` block to `ServiceAlertRepository.test.ts`, mirroring the `getActiveAlertsForRoute` block Plan 06-01 added, covering every relevant `<behavior>` bullet above.

    In `src/server/api/models/StopDiscovery.ts`: import `ServiceAlertSummary` and `StopWithAlerts` from `./ServiceAlertSummary`. Change `RouteDirectionStops.stops` field type from `BusStop[]` to `StopWithAlerts[]`. Add an `alerts: ServiceAlertSummary[]` field to the `NearbyStop` interface (after `distance`).

    In `src/server/api/services/StopService.ts`: change `createStopService(repository: BusDataRepository)` to `createStopService(repository: BusDataRepository, serviceAlertRepository: ServiceAlertRepository)` (import `ServiceAlertRepository` as a type from `../repositories`). Add a private `attachStopAlerts(stop: BusStop): StopWithAlerts` helper computing `const alerts = mapToServiceAlertSummaries(serviceAlertRepository.getActiveAlertsForStop(stop.id));` and returning `{ id: stop.id, name: stop.name, code: stop.code, lat: stop.lat, lon: stop.lon, alerts }` (explicit field picks, matching the existing `getNearbyStops` style rather than spreading the `BusStop` class instance). In `getStopsForRoute`, change the `stops: direction.stops` line to `stops: direction.stops.map(attachStopAlerts)`. In `getNearbyStops`, add `alerts: mapToServiceAlertSummaries(serviceAlertRepository.getActiveAlertsForStop(stop.id))` to the object returned inside the existing `.map()` callback (before the `.filter()`/`.sort()`/`.slice()` chain), leaving `distance` computation and the rest of the pipeline untouched. Import `mapToServiceAlertSummaries` from `./serviceAlertMapping`.

    In `StopService.test.ts`: add a `makeMockAlertRepo = () => ({ getActiveAlertsForRoute: vi.fn().mockReturnValue([]), getActiveAlertsForStop: vi.fn().mockReturnValue([]) })` helper alongside the existing `makeMockRepo()`. Using a global find-replace (Edit tool `replace_all`), change every `createStopService(mockRepo as never)` call site (there are 24) to `createStopService(mockRepo as never, makeMockAlertRepo() as never)`. Update the stale exact-equality assertions: in `getStopsForRoute` > "returns the grouped array with correct directionId/title/stops per direction", change the expected `stops: [northStop]`/`stops: [southStop]` to the mapped shape `stops: [{ id: northStop.id, name: northStop.name, code: northStop.code, lat: northStop.lat, lon: northStop.lon, alerts: [] }]` (and the southStop equivalent); in "does not dedupe a stop shared by two directions", change both `toContainEqual(sharedStop)` assertions to `toContainEqual({ id: sharedStop.id, name: sharedStop.name, code: sharedStop.code, lat: sharedStop.lat, lon: sharedStop.lon, alerts: [] })`; in "preserves the direction's real stop sequence order", change `toEqual([stopC, stopA, stopB])` to the three mapped-shape objects in the same order; in `getNearbyStops` > "defaults to radius 0.5 miles and count 10 when options are omitted", add `alerts: []` to the expected object. Add new tests: `getStopsForRoute` embeds `alerts` per stop via `getActiveAlertsForStop(stop.id)`, `[]` when unmatched, and calls `getActiveAlertsForStop` once per stop *occurrence* (twice for a stop shared across 2 directions — adjacency); `getNearbyStops` embeds `alerts` per stop and preserves distance-ascending sort order when alerts differ per stop (ordering).

    In `src/server/api/routes/busRoutes.ts`, change `const stopService = createStopService(BusDataRepository.getInstance());` to `const stopService = createStopService(BusDataRepository.getInstance(), ServiceAlertRepository.getInstance());` (the `ServiceAlertRepository` import was already added to this file's `../repositories` import in Plan 06-01). In `src/server/api/routes/stopRoutes.ts`, add `ServiceAlertRepository` to the existing `import { BusDataRepository } from "../repositories";` line and change `const service = createStopService(BusDataRepository.getInstance());` to `const service = createStopService(BusDataRepository.getInstance(), ServiceAlertRepository.getInstance());`.
  </action>
  <verify>
    <automated>bun run test -- src/server/api/services/StopService.test.ts src/server/api/repositories/ServiceAlertRepository.test.ts &amp;&amp; bun run build</automated>
    <fails_when>either command exits non-zero, the vitest summary line contains "failed" (case-insensitive), or tsc writes a compile error to stderr (e.g. a TS2345/TS2554 argument-count mismatch from a missed createStopService call site)</fails_when>
  </verify>
  <done>GET /api/v1/routes/:shortName/stops and GET /api/v1/stops/nearby (via StopService.getStopsForRoute/getNearbyStops) return stop objects with a trimmed alerts array matched by stop id (D-01, D-02, D-03), agency-wide alerts are excluded (D-04), distance-ascending sort order in stops/nearby is unaffected, and createStopService receives ServiceAlertRepository only via its factory parameter.</done>
  <acceptance_criteria>
    - `grep -n "getActiveAlertsForStop" src/server/api/repositories/ServiceAlertRepository.test.ts` shows a dedicated describe block.
    - `grep -n "alerts: ServiceAlertSummary\[\]" src/server/api/models/StopDiscovery.ts` shows the new `NearbyStop.alerts` field.
    - `grep -c "createStopService(mockRepo as never)$" src/server/api/services/StopService.test.ts` (single-arg form) is 0 — every call site was updated to the two-arg form.
    - `bun run test -- src/server/api/services/StopService.test.ts src/server/api/repositories/ServiceAlertRepository.test.ts` exits 0.
    - `bun run build` exits 0 with no TypeScript errors.
  </acceptance_criteria>
  <reversibility rating="reversible">D-03's stop-ID matching is a pure filter predicate over already-ingested, non-persisted alert data (re-fetched every 5-minute poll); changing the match key later is a code-only edit with no data migration.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Route-wiring regression + full-phase verification</name>
  <files>src/server/api/routes/busRoutes.test.ts, src/server/api/routes/stopRoutes.test.ts</files>
  <read_first>
    src/server/api/routes/busRoutes.ts (post-Task-1 stopService DI wiring)
    src/server/api/routes/busRoutes.test.ts (existing vi.mock("../services/StopService", ...) setup, called once here for the /:shortName/stops route)
    src/server/api/routes/stopRoutes.ts (post-Task-1 DI wiring)
    src/server/api/routes/stopRoutes.test.ts (getMockService() uses mock.results[1] since busRoutes.ts instantiates createStopService first — see the file's existing comment)
  </read_first>
  <action>
    In `busRoutes.test.ts`, add an assertion that the first `createStopService` invocation (`vi.mocked(createStopService).mock.calls[0]`) has length 2 — proving `busRoutes.ts` wires `ServiceAlertRepository` into its `stopService` instantiation. In `stopRoutes.test.ts`, add an assertion that the second `createStopService` invocation (`vi.mocked(createStopService).mock.calls[1]`) has length 2 — proving `stopRoutes.ts`'s own wiring, consistent with this file's existing `mock.results[1]` convention for isolating "the instance wired to this router." These lock in the prohibition that `StopService` must receive `ServiceAlertRepository` via factory-DI rather than importing the singleton directly.

    Then run the full test suite (with coverage, to confirm the 80% threshold in CLAUDE.md still holds after this phase's new files/branches), lint, and build as the closing gate for the whole phase (ALRT-05 through ALRT-08 all implemented).
  </action>
  <verify>
    <automated>bun run test:coverage &amp;&amp; bun run lint &amp;&amp; bun run build</automated>
    <fails_when>any command exits non-zero, the vitest summary reports a failed test, coverage drops below the 80% branches/functions/lines/statements threshold, or Biome (`lint`) reports any error-level finding</fails_when>
  </verify>
  <done>busRoutes.test.ts and stopRoutes.test.ts both assert createStopService is invoked with 2 arguments (route/stop repository + service-alert repository), and the full test suite (with coverage), lint, and build all pass — completing ALRT-05 through ALRT-08.</done>
  <acceptance_criteria>
    - `grep -n "mock.calls\[0\]).toHaveLength(2)" src/server/api/routes/busRoutes.test.ts` shows the new stopService wiring assertion.
    - `grep -n "mock.calls\[1\]).toHaveLength(2)" src/server/api/routes/stopRoutes.test.ts` shows the new wiring assertion.
    - `bun run test:coverage` exits 0 with all four coverage metrics at or above 80%.
    - `bun run lint` exits 0.
    - `bun run build` exits 0.
  </acceptance_criteria>
</task>

</tasks>

## Artifacts This Phase Produces

Reused from Plan 06-01 (not redefined here): `ServiceAlertSummary`, `RouteWithAlerts`, `StopWithAlerts` (`src/server/api/models/ServiceAlertSummary.ts`); `mapToServiceAlertSummary`/`mapToServiceAlertSummaries` (`src/server/api/services/serviceAlertMapping.ts`); `ServiceAlertRepository.getActiveAlertsForStop` (implemented in 06-01, tested here).

New in this plan:
- Updated model fields: `NearbyStop.alerts: ServiceAlertSummary[]` in `src/server/api/models/StopDiscovery.ts`; `RouteDirectionStops.stops` type changed from `BusStop[]` to `StopWithAlerts[]`
- Updated signature: `createStopService(repository: BusDataRepository, serviceAlertRepository: ServiceAlertRepository): StopService` — `getStopsForRoute`/`getNearbyStops` return types gain per-stop `alerts`
- Updated DI wiring: `src/server/api/routes/busRoutes.ts`'s `stopService` instantiation and `src/server/api/routes/stopRoutes.ts`'s `service` instantiation both pass `ServiceAlertRepository.getInstance()` as the second argument to `createStopService`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Public HTTP client -> stop endpoints | Unauthenticated internet clients call `GET /api/v1/routes/:shortName/stops` / `GET /api/v1/stops/nearby`; the server now returns embedded alert content on a per-stop basis. |
| DASH/Swiftly upstream -> ServiceAlertRepository | Unchanged from Plan 06-01 — ingestion is owned by Phase 5. |
| Stop/alert ID matching (D-03) | `ServiceAlert.informedStopIds` (upstream-sourced) is matched against internal `BusStop.id` (DASH `/info` API-sourced) inside `ServiceAlertRepository.getActiveAlertsForStop`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Tampering | `ServiceAlertRepository.getActiveAlertsForStop` (ID matching) | low | accept | Same rationale as T-06-01: `informedStopIds` and `BusStop.id` both originate from the trusted DASH/Swiftly system; no user-controlled input reaches the match. |
| T-06-05 | Information Disclosure | `GET /api/v1/routes/:shortName/stops`, `GET /api/v1/stops/nearby` (alert embedding) | low | accept | Same rationale as T-06-02, applied per-stop instead of per-route — public transit-service information, trimmed per D-01. |
| T-06-06 | Denial of Service | `StopService.getStopsForRoute`/`getNearbyStops` (O(stops x active-alerts) recompute per request) | medium | mitigate | Same mitigation as T-06-03: bounded by Phase 5's 5-minute-poll-refreshed in-memory alert set; embedding is attached inside the existing per-request `.map()` pass with no new network calls or unbounded loops. |

</threat_model>

<verification>
- `bun run test -- src/server/api/services/StopService.test.ts src/server/api/repositories/ServiceAlertRepository.test.ts` passes.
- `bun run test:coverage` (full suite) passes at or above the 80% threshold.
- `bun run lint` and `bun run build` both pass.
- Manual spot-check: `GET /api/v1/stops/nearby?lat=...&lng=...` for a stop with a matching active alert returns an `alerts` array containing a trimmed summary object; a stop with no matching alert returns `alerts: []`.
</verification>

<success_criteria>
- ALRT-07: `GET /api/v1/routes/:shortName/stops` includes each stop's currently-active alerts.
- ALRT-08: `GET /api/v1/stops/nearby` includes each returned stop's currently-active alerts.
- Stops with no active alerts return `alerts: []`, never an omitted field (D-02).
- `StopService` receives `ServiceAlertRepository` via factory-DI, never via direct singleton import.
- All of Phase 6's requirements (ALRT-05 through ALRT-08) are complete; full test suite (with coverage), lint, and build all pass.
</success_criteria>

<output>
Create `.planning/phases/06-alerts-surfaced-on-routes-stops-predictions/06-02-SUMMARY.md` when done
</output>
