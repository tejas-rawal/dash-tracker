# Deferred Items — Phase 6

Out-of-scope issues discovered during execution, not fixed per the SCOPE BOUNDARY rule
(only issues directly caused by the current task's changes are auto-fixed).

## 06-01

_All items resolved — see git history / RETROSPECTIVE.md. Previously: `bun run lint` failed on
`.planning/config.json` (pre-existing, unrelated to this plan); fixed at v0.4 milestone close
(2026-09-08) by adding `.planning` and `.gsd` to `biome.json`'s `files.ignore`._
