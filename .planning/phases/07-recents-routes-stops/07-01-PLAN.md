---
phase: 07-recents-routes-stops
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/api/models/Prediction.ts
  - src/server/api/models/PersistedEntity.ts
  - src/server/api/services/PredictionService.ts
  - src/server/api/services/PredictionService.test.ts
  - src/server/api/controllers/PredictionController.ts
  - src/server/api/controllers/PredictionController.test.ts
  - src/server/api/routes/predictionRoutes.ts
  - src/server/api/routes/predictionRoutes.test.ts
  - src/server/api/services/PredictionStreamService.test.ts
  - src/server/api/repositories/FavoritesRecentsRepository.ts
  - src/server/api/repositories/FavoritesRecentsRepository.test.ts
  - src/server/api/services/RecentsService.ts
  - src/server/api/services/RecentsService.test.ts
  - src/server/api/controllers/RecentsController.ts
  - src/server/api/controllers/RecentsController.test.ts
  - src/server/api/routes/recentRoutes.ts
  - src/server/api/routes/recentRoutes.test.ts
  - src/server/api/routes/index.ts
autonomous: true
requirements: [RECENT-01, RECENT-02, RECENT-03, RECENT-04, RECENT-05, RECENT-06]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A prediction lookup made with an explicit route query param (e.g. GET /api/v1/predictions?stop=stop-1&route=1A) auto-logs that route as a recent via FavoritesRecentsRepository.upsertRecent(deviceId, \"route\", \"1A\"); an unfiltered stop-only lookup (no route param) never calls upsertRecent with entityType \"route\" (RECENT-01, ROADMAP Phase 7 SC #1)."
    - "Every prediction lookup — the stop param is always explicit per the existing controller validation that 400s without it — auto-logs that stop as a recent via upsertRecent(deviceId, \"stop\", stopId) (RECENT-02, SC #2)."
    - "Re-viewing an already-recent route or stop bumps its existing row to the top via the existing Phase-5 INSERT ... ON CONFLICT DO UPDATE upsert instead of creating a duplicate row (RECENT-03, SC #3)."
    - "The combined recents list for one device (routes + stops together) never exceeds 5 rows: FavoritesRecentsRepository.upsertRecent evicts the oldest row(s) by viewed_at whenever a write would push the count above 5 — verified by writing 6 distinct recents for one device and asserting exactly 5 remain, oldest evicted first (RECENT-04, SC #3)."
    - "GET /api/v1/recents returns one combined array mixing routes and stops, each entry shaped {entityType, viewedAt, entity}, ordered viewedAt DESC across both entity types together, hydrated with full route/stop details from BusDataRepository (RECENT-05, SC #4)."
    - "GET /api/v1/recents for a device with zero recents returns 200 [], never 404; a recent whose entityId no longer resolves via BusDataRepository is silently skipped from the response, mirroring the Favorites hydration contract (RECENT-05)."
    - "Opening GET /api/v1/predictions/stream (with or without an X-Device-Id header) never calls FavoritesRecentsRepository.upsertRecent — PredictionStreamService and PredictionStreamController receive zero changes from this plan, and their existing calls to getPredictionsForStop(stopId) never supply a deviceId, so the logging call site structurally does not exist on that path (RECENT-06, SC #5)."
    - statement: "The recents-logging side effect never delays or breaks the primary prediction response: getPredictionsForStop resolves and returns its StopPredictionsResponse even while the mocked FavoritesRecentsRepository.upsertRecent call is still pending, and even when that call outright rejects (producing only a logger.warn call, never a thrown error or rejected response) — verified by an automated test that asserts the response resolves without ever awaiting the recents write (SC #5)."
      verification: backstop
    - "A prediction lookup with a missing, empty, or whitespace-only X-Device-Id header still returns its normal 200 response, never calls upsertRecent, and never 400s — requireDeviceId middleware is never applied to predictionRoutes.ts (D-01 from 07-CONTEXT.md)."
    - "GET /api/v1/recents (unlike GET /api/v1/predictions) requires X-Device-Id: a missing or empty header returns 400 {error:\"Bad Request\", details:\"X-Device-Id header is required\"} via the reused requireDeviceId middleware (D-08)."
    - "All existing v0.2 endpoints (routes, stop discovery, predictions payload shape, SSE) and PredictionStreamService/PredictionStreamController continue to respond exactly as before — their existing test suites pass unchanged (non-regression alongside SC #5)."
  artifacts:
    - path: "src/server/api/services/RecentsService.ts"
      provides: "listRecents(deviceId) business logic — hydrates recents from BusDataRepository, mirrors FavoritesService.listFavorites exactly"
      exports: ["RecentsService", "createRecentsService"]
    - path: "src/server/api/controllers/RecentsController.ts"
      provides: "listRecents RequestHandler — device-id resolution, error-status mapping, mirrors FavoritesController"
      exports: ["RecentsController", "createRecentsController"]
    - path: "src/server/api/routes/recentRoutes.ts"
      provides: "Router mounting requireDeviceId + GET /recents, wired to RecentsController via factory DI"
      exports: ["default (Router)"]
    - path: "src/server/api/models/PersistedEntity.ts"
      provides: "HydratedRecent type — the nested {entityType, viewedAt, entity} response shape — modified, not new file"
      exports: ["HydratedRecent (new, alongside existing EntityType/FavoriteRecord/RecentRecord/HydratedFavorite)"]
    - path: "src/server/api/repositories/FavoritesRecentsRepository.ts"
      provides: "upsertRecent extended with cap-at-5, oldest-evicted-first eviction — same public signature, modified not new"
      exports: ["FavoritesRecentsRepository (extended, same export)"]
    - path: "src/server/api/services/PredictionService.ts"
      provides: "createPredictionService gains a second FavoritesRecentsRepository parameter; getPredictionsForStop fire-and-forget-logs stop/route recents"
      exports: ["PredictionService", "createPredictionService (signature changed: now 2 params)"]
    - path: "src/server/api/controllers/PredictionController.ts"
      provides: "Resolves an optional X-Device-Id (never 400s on absence) and passes it through to the service"
      exports: ["PredictionController", "createPredictionController (same signature)"]
    - path: "src/server/api/models/Prediction.ts"
      provides: "PredictionOptions gains an optional deviceId field, the internal-only signal for fire-and-forget recents logging"
      exports: ["PredictionOptions (extended)"]
  key_links:
    - from: "src/server/api/routes/index.ts"
      to: "src/server/api/routes/recentRoutes.ts"
      via: "router.use(\"/recents\", recentRoutes) mounted alongside the existing routes/predictions/stops/favorites lines"
      pattern: "router\\.use\\(\"/recents\", recentRoutes\\)"
    - from: "src/server/api/routes/recentRoutes.ts"
      to: "src/server/api/middleware/requireDeviceId.ts"
      via: "router.use(requireDeviceId) applied before the recents GET endpoint, reused unmodified from Phase 6"
      pattern: "router\\.use\\(requireDeviceId\\)"
    - from: "src/server/api/controllers/RecentsController.ts"
      to: "src/server/api/services/RecentsService.ts"
      via: "createRecentsController(service) calls service.listRecents"
      pattern: "service\\.listRecents"
    - from: "src/server/api/services/RecentsService.ts"
      to: "src/server/api/repositories/FavoritesRecentsRepository.ts"
      via: "recentsRepository.listRecents(deviceId) for hydration source data"
      pattern: "recentsRepository\\.listRecents"
    - from: "src/server/api/routes/predictionRoutes.ts"
      to: "src/server/api/services/PredictionService.ts"
      via: "createPredictionService(BusDataRepository.getInstance(), FavoritesRecentsRepository.getInstance())"
      pattern: "createPredictionService\\(BusDataRepository\\.getInstance\\(\\), FavoritesRecentsRepository\\.getInstance\\(\\)\\)"
    - from: "src/server/api/services/PredictionService.ts"
      to: "src/server/api/repositories/FavoritesRecentsRepository.ts"
      via: "recentsRepository.upsertRecent(deviceId, entityType, entityId) invoked without await, .catch()-guarded"
      pattern: "recentsRepository\\.upsertRecent"
    - from: "src/server/api/services/PredictionStreamService.ts"
      to: "src/server/api/services/PredictionService.ts"
      via: "getPredictionsForStop(stopId) called with no options/deviceId argument — the structural reason SSE never logs a recent"
      pattern: "getPredictionsForStop\\(stopId\\)"
  prohibitions:
    - statement: "predictionRoutes.ts must never gain a requireDeviceId (or any other device-id-validation) middleware — D-01 requires GET /api/v1/predictions to stay exactly as it is today; adding validation there would silently turn an optional signal into a required one and break existing unauthenticated prediction clients."
      status: flagged
      verification: unverified
    - statement: "The recents-logging call inside getPredictionsForStop must never be awaited, directly or via a wrapping try/await — D-03's entire non-blocking guarantee (and RECENT-06/SC#5's 'never delays or breaks the response' contract) depends on it firing fire-and-forget with only a .catch(), never an await."
      status: flagged
      verification: unverified
    - statement: "PredictionStreamService.ts and PredictionStreamController.ts must never gain a recents-logging call site, conditional or otherwise — D-04 requires the call site to structurally not exist on the SSE path, not merely be gated by a runtime check."
      status: flagged
      verification: unverified
---

<objective>
Wire automatic recents tracking on top of Phase 5's `FavoritesRecentsRepository` and Phase 6's device-id/hydration patterns: every REST prediction lookup fire-and-forget-logs its stop (and, when explicit, its route) as a recent, capped at 5 combined entries per device with oldest-evicted-first, listable through a new device-scoped `GET /api/v1/recents` endpoint — all without ever touching the SSE stream path or slowing/breaking the underlying prediction response.

Purpose: Satisfies RECENT-01 through RECENT-06 — the entirety of ROADMAP Phase 7 — completing the v0.3 milestone (Favorited & Recent Routes) alongside the already-shipped Phase 5/6 work.
Output: `PredictionService`/`PredictionController` extended for fire-and-forget auto-logging, `FavoritesRecentsRepository.upsertRecent` extended with cap-at-5 eviction, and a new `RecentsService`/`RecentsController`/`recentRoutes.ts` HTTP surface mounted at `/recents`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-recents-routes-stops/07-CONTEXT.md
@src/server/api/services/FavoritesService.ts
@src/server/api/controllers/FavoritesController.ts
@src/server/api/routes/favoriteRoutes.ts
@src/server/api/routes/index.ts
@src/server/api/services/PredictionService.ts
@src/server/api/controllers/PredictionController.ts
@src/server/api/services/PredictionStreamService.ts
@src/server/api/repositories/FavoritesRecentsRepository.ts
@src/server/api/models/PersistedEntity.ts
@src/server/api/models/Prediction.ts
</context>

## Artifacts This Phase Produces

New and modified symbols, for downstream drift verification:

- **src/server/api/models/Prediction.ts** (modified) — `PredictionOptions` gains `deviceId?: string`, an internal-only field never validated by the controller and never part of the DASH API URL.
- **src/server/api/services/PredictionService.ts** (modified) — `createPredictionService(repository: BusDataRepository, recentsRepository: FavoritesRecentsRepository): PredictionService` (signature grows from 1 to 2 params); `getPredictionsForStop` gains a private `recordRecentView(deviceId, stopId, routeId?)` helper invoked fire-and-forget (no `await`, `.catch()`-guarded) after the response is built.
- **src/server/api/controllers/PredictionController.ts** (modified) — new private `resolveOptionalDeviceId(req): string | undefined` helper; `getPredictions` passes `deviceId` through in the options object handed to the service.
- **src/server/api/routes/predictionRoutes.ts** (modified) — `createPredictionService` call site gains `FavoritesRecentsRepository.getInstance()` as its second argument.
- **src/server/api/repositories/FavoritesRecentsRepository.ts** (modified, existing class) — `upsertRecent` gains a follow-up `DELETE FROM recents WHERE device_id = @deviceId AND id NOT IN (SELECT id FROM recents WHERE device_id = @deviceId ORDER BY viewed_at DESC LIMIT 5)` after its existing insert/upsert statement. Public signature unchanged.
- **src/server/api/models/PersistedEntity.ts** (modified) — new `export interface HydratedRecent { entityType: EntityType; viewedAt: string; entity: BusRoute | BusStop; }`.
- **src/server/api/services/RecentsService.ts** (new file) — `export interface RecentsService { listRecents(deviceId: string): Promise<HydratedRecent[]>; }`, `export function createRecentsService(recentsRepository: FavoritesRecentsRepository, busDataRepository: BusDataRepository): RecentsService`.
- **src/server/api/controllers/RecentsController.ts** (new file) — `export interface RecentsController { listRecents: RequestHandler; }`, `export function createRecentsController(service: RecentsService): RecentsController`.
- **src/server/api/routes/recentRoutes.ts** (new file) — `export default router`, mounts `requireDeviceId` then `GET /`.
- **src/server/api/routes/index.ts** (modified) — new `import recentRoutes from "./recentRoutes";` and `router.use("/recents", recentRoutes);`.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end stop-recent auto-logging on a prediction lookup — fire-and-forget, SSE-safe</name>
  <precondition>src/server/api/repositories/FavoritesRecentsRepository.ts exports a working upsertRecent(deviceId, entityType, entityId) method and FavoritesRecentsRepository.getInstance() is already wired into src/server/app.ts's startup initialize() sequence (Phase 05, completed and merged) — this task adds no repository lifecycle changes, only reads the existing singleton via getInstance() and calls its existing method.</precondition>
  <read_first>
    .planning/phases/07-recents-routes-stops/07-CONTEXT.md (D-01 through D-05 — device-id-optional-on-predictions, fire-and-forget mechanics, SSE exclusion); src/server/api/services/FavoritesService.ts and src/server/api/controllers/FavoritesController.ts (the resolveDeviceId array-normalization idiom this task's *optional* variant mirrors); src/server/api/services/PredictionService.ts (existing getPredictionsForStop — the exact point after building `response` where the fire-and-forget call attaches); src/server/api/controllers/PredictionController.ts (existing getPredictions — where the options object `{ number, route }` is built and must gain `deviceId`); src/server/api/routes/predictionRoutes.ts (existing createPredictionService(BusDataRepository.getInstance()) call site); src/server/api/routes/favoriteRoutes.ts (repository-instantiation-at-module-load pattern to mirror for the new second argument); src/server/api/services/PredictionStreamService.ts (both call sites of getPredictionsForStop — confirm they pass only `stopId`, never an options object, which is what makes zero changes to this file sufficient for RECENT-06); src/server/api/repositories/FavoritesRecentsRepository.ts (upsertRecent's exact signature — this task calls it, does not modify it); src/server/api/models/Prediction.ts (PredictionOptions — the interface to extend).
  </read_first>
  <files>src/server/api/models/Prediction.ts, src/server/api/services/PredictionService.ts, src/server/api/services/PredictionService.test.ts, src/server/api/controllers/PredictionController.ts, src/server/api/controllers/PredictionController.test.ts, src/server/api/routes/predictionRoutes.ts, src/server/api/routes/predictionRoutes.test.ts, src/server/api/services/PredictionStreamService.test.ts</files>
  <action>
In `src/server/api/models/Prediction.ts`, add `deviceId?: string;` to the existing `PredictionOptions` interface alongside `number?`/`route?`. This field is internal-only: never validated, never required, never forwarded to the DASH API URL.

In `src/server/api/services/PredictionService.ts`, change the factory signature from `createPredictionService(repository: BusDataRepository)` to `createPredictionService(repository: BusDataRepository, recentsRepository: FavoritesRecentsRepository)`, extending the existing `import type { BusDataRepository } from "../repositories";` line to `import type { BusDataRepository, FavoritesRecentsRepository } from "../repositories";`. Add a private `async function recordRecentView(deviceId: string, stopId: string, routeId?: string): Promise<void> { await recentsRepository.upsertRecent(deviceId, "stop", stopId); }` (the `routeId` parameter is accepted now but unused until Task 2 activates its branch, to keep the call site stable across both tasks). Inside `getPredictionsForStop`, after the `const response = { ... }`-equivalent return value is fully constructed and before it is returned, add: if `options.deviceId` is a defined, non-empty (post-`.trim()`) string, call `recordRecentView(options.deviceId, stopId, options.route)` WITHOUT `await`, chaining `.catch((error) => { const message = error instanceof Error ? error.message : "Unknown error"; logger.warn(\`Failed to record recents for device ${options.deviceId}: ${message}\`); })`. The function then returns its response value immediately regardless of the fire-and-forget promise's state — this is the exact mechanism satisfying "never delays or breaks the response" (D-03). Do not modify `src/server/api/services/PredictionStreamService.ts` or `src/server/api/controllers/PredictionStreamController.ts` at all: both existing call sites invoke `getPredictionsForStop(stopId)` with no second argument, so `options` defaults to `{}` and `options.deviceId` is always `undefined` on that path — this is precisely why RECENT-06 requires zero changes to those two files rather than a runtime SSE-vs-REST check (D-04).

In `src/server/api/controllers/PredictionController.ts`, add a private `function resolveOptionalDeviceId(req: Request): string | undefined { const raw = req.headers["x-device-id"]; const value = Array.isArray(raw) ? raw[0] : raw; return value !== undefined && value.trim().length > 0 ? value : undefined; }` — same array-normalization idiom as `FavoritesController.resolveDeviceId`, but returning `undefined` instead of assuming presence, and never responding 400 (D-01: the predictions endpoint keeps accepting requests with no device id at all). In `getPredictions`, change the service call from `service.getPredictionsForStop(stop, { number, route })` to `service.getPredictionsForStop(stop, { number, route, deviceId: resolveOptionalDeviceId(req) })`. Do not add `requireDeviceId` (or any device-id middleware) to `predictionRoutes.ts`.

In `src/server/api/routes/predictionRoutes.ts`, extend `import { BusDataRepository } from "../repositories";` to `import { BusDataRepository, FavoritesRecentsRepository } from "../repositories";` and change `const service = createPredictionService(BusDataRepository.getInstance());` to `const service = createPredictionService(BusDataRepository.getInstance(), FavoritesRecentsRepository.getInstance());`.

Update `src/server/api/services/PredictionService.test.ts`: add `const makeMockRecentsRepo = () => ({ upsertRecent: vi.fn().mockResolvedValue(undefined) });` and change every existing `createPredictionService(mockRepo as never)` call to `createPredictionService(mockRepo as never, makeMockRecentsRepo() as never)` (or a shared `mockRecentsRepo` per test, following this file's existing `makeMockRepo()`-per-test convention). Add new test cases under `describe("getPredictionsForStop", ...)`: (a) calling with `{ deviceId: "device-a" }` results in `mockRecentsRepo.upsertRecent` called with `("device-a", "stop", "stop-1")`; (b) calling with no `deviceId` option never calls `upsertRecent`; (c) calling with `deviceId: "  "` (whitespace-only, defensive — mirrors the controller's own guard) never calls `upsertRecent` if the controller-level guard were bypassed; (d) `getPredictionsForStop` still resolves its `StopPredictionsResponse` when `mockRecentsRepo.upsertRecent` returns a `Promise` that is never resolved within the test (assert the awaited result is available without ever resolving/advancing that pending promise); (e) `getPredictionsForStop` still resolves successfully when `mockRecentsRepo.upsertRecent` rejects, and `logger.warn` (already mocked via the file's `vi.mock("../../config", ...)`) is called exactly once with a message containing the device id — no unhandled rejection surfaces.

Update `src/server/api/controllers/PredictionController.test.ts`: change `makeMockReq` to `const makeMockReq = (query: Record<string, string> = {}, headers: Record<string, string | string[] | undefined> = {}): Request => ({ query, headers }) as unknown as Request;` (default empty headers preserves all existing calls). Add new test cases: `getPredictions` with header `{ "x-device-id": "device-a" }` calls `service.getPredictionsForStop` with an options object containing `deviceId: "device-a"`; without that header, the options object's `deviceId` is `undefined`; with `{ "x-device-id": "   " }` (whitespace-only), `deviceId` is also `undefined`.

Update `src/server/api/routes/predictionRoutes.test.ts`: since `createPredictionService` is already `vi.mock`'d in this file, add one wiring assertion in a new small `describe` block (or alongside an existing test): `expect(vi.mocked(createPredictionService).mock.calls[0]).toHaveLength(2);` confirming `predictionRoutes.ts` now passes two arguments at module load.

Update `src/server/api/services/PredictionStreamService.test.ts`: add a regression test in the existing `describe("PredictionStreamService", ...)` block asserting `mockPredictionService.getPredictionsForStop` is called with exactly one argument on both the initial `subscribe` fetch and a subsequent timer-driven `poll` tick — e.g. `expect(mockPredictionService.getPredictionsForStop).toHaveBeenCalledWith("stop-1")` (single-arg form, not `expect.any(Object)` as a second arg) — locking in that the stream path structurally never supplies a `deviceId`.
  </action>
  <acceptance_criteria>
    - `getPredictionsForStop("stop-1", { deviceId: "device-a" })` results in the mocked recents repository's `upsertRecent` being called with `("device-a", "stop", "stop-1")` (PredictionService.test.ts).
    - `getPredictionsForStop("stop-1")` (no `deviceId`) results in zero calls to `upsertRecent` (PredictionService.test.ts).
    - `getPredictionsForStop` resolves and returns the built response even when the mocked `upsertRecent` call rejects; `logger.warn` is called exactly once referencing the failure (PredictionService.test.ts).
    - `PredictionController.getPredictions` with an `X-Device-Id` header present calls the service with `deviceId` set to that header's value; without the header, or with a whitespace-only header, `deviceId` is `undefined` (PredictionController.test.ts).
    - `PredictionStreamService`'s `subscribe` and `poll` call `predictionService.getPredictionsForStop` with exactly one argument, never a second options/deviceId argument (PredictionStreamService.test.ts) — locks in RECENT-06 structurally.
    - `git diff --stat -- src/server/api/services/PredictionStreamService.ts src/server/api/controllers/PredictionStreamController.ts` is empty (D-04 — zero changes to the SSE path).
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- src/server/api/services/PredictionService.test.ts src/server/api/controllers/PredictionController.test.ts src/server/api/routes/predictionRoutes.test.ts src/server/api/services/PredictionStreamService.test.ts && bun run build</automated>
    <fails_when>non-zero exit from either command, or the vitest summary line shows a nonzero "failed" count, or "error TS" appears in the bun run build output</fails_when>
  </verify>
  <done>A REST prediction lookup with a device id fire-and-forget-logs the stop as a recent via the existing FavoritesRecentsRepository.upsertRecent, never blocks or breaks the response even on a rejected write, and the SSE stream path remains completely unmodified and structurally incapable of triggering it — committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route-recent logging + cap-at-5 eviction (RECENT-01 negative case, RECENT-03, RECENT-04)</name>
  <reversibility rating="costly">Once old recents rows are evicted by the cap-at-5 DELETE, they cannot be recovered if the cap policy changes later (e.g. raising the limit) — per D-06's explicit rationale in 07-CONTEXT.md.</reversibility>
  <read_first>
    .planning/phases/07-recents-routes-stops/07-CONTEXT.md (D-05, D-06, D-07 — route-recent conditionality, exact eviction SQL shape, unchanged upsertRecent signature); src/server/api/services/PredictionService.ts (Task 1's committed recordRecentView helper and its `options.route !== undefined` sibling check already used in `buildDashApiUrl` — mirror that exact condition, don't invent a new one); src/server/api/repositories/FavoritesRecentsRepository.ts (the existing upsertRecent's prepared-statement idiom — `(this.db as DatabaseInstance).prepare(...).run(...)` after `this.assertInitialized()` — the eviction statement follows the same idiom); src/server/api/repositories/FavoritesRecentsRepository.test.ts (existing `describe("concurrency", ...)` and `describe("recents", ...)` blocks — the TEST_DB_PATH/vi.mock setup and Promise.all idiom to extend, not rewrite).
  </read_first>
  <files>src/server/api/services/PredictionService.ts, src/server/api/services/PredictionService.test.ts, src/server/api/repositories/FavoritesRecentsRepository.ts, src/server/api/repositories/FavoritesRecentsRepository.test.ts</files>
  <behavior>
    - `recordRecentView(deviceId, stopId, routeId)` calls `recentsRepository.upsertRecent(deviceId, "route", routeId)` in addition to the stop call when `routeId !== undefined`, and does not call it when `routeId` is `undefined`.
    - `getPredictionsForStop("stop-1", { deviceId: "device-a", route: "1A" })` results in two `upsertRecent` calls: `("device-a", "stop", "stop-1")` and `("device-a", "route", "1A")`.
    - `getPredictionsForStop("stop-1", { deviceId: "device-a" })` (no `route`) results in exactly one `upsertRecent` call, for `"stop"` only.
    - `FavoritesRecentsRepository.upsertRecent`, after its insert/update, deletes any rows for that device beyond the 5 most-recently-viewed (by `viewed_at DESC`), regardless of entity-type mix.
    - Writing a 6th distinct recent for a device evicts exactly the oldest one; the remaining 5 are the 5 most recently written/bumped.
    - Re-upserting (bumping) one of an existing 5 recents does not change the row count (stays at 5, no new row, no incorrect eviction).
    - Eviction is scoped per device: a 6th write for device A never removes any of device B's existing rows.
  </behavior>
  <action>
Extend `PredictionService.ts`'s `recordRecentView` body to `async function recordRecentView(deviceId: string, stopId: string, routeId?: string): Promise<void> { const writes = [recentsRepository.upsertRecent(deviceId, "stop", stopId)]; if (routeId !== undefined) { writes.push(recentsRepository.upsertRecent(deviceId, "route", routeId)); } await Promise.all(writes); }` — mirrors the exact `options.route !== undefined` condition `buildDashApiUrl` already uses for the DASH API URL, per D-05. The call site inside `getPredictionsForStop` (added in Task 1) already passes `options.route` as the third argument, so no call-site change is needed here — only the helper's internal branching changes.

In `FavoritesRecentsRepository.ts`'s `upsertRecent` method, immediately after the existing `INSERT ... ON CONFLICT (...) DO UPDATE ...` `.run({...})` call, add: `(this.db as DatabaseInstance).prepare("DELETE FROM recents WHERE device_id = @deviceId AND id NOT IN (SELECT id FROM recents WHERE device_id = @deviceId ORDER BY viewed_at DESC LIMIT 5)").run({ deviceId });` — the exact SQL shape specified by D-06, run unconditionally on every `upsertRecent` call so callers (the service) never need to think about the cap (D-07: the public `upsertRecent(deviceId, entityType, entityId)` signature itself does not change).

Extend `FavoritesRecentsRepository.test.ts` with a new `describe("eviction", ...)` block (sibling to the existing `describe("recents", ...)`), with a `beforeEach` calling `await repo.initialize()` matching the sibling blocks: (1) writing 6 distinct `(entityType, entityId)` recents for `"device-a"` (mixing route/stop) asserts `listRecents("device-a")` returns exactly 5 rows and the first-written entity is absent; (2) re-upserting an existing (already-in-top-5) recent among 5 total asserts the row count stays at 5, not 6; (3) with `"device-b"` already holding 5 recents, `"device-a"` writing a 6th recent asserts `listRecents("device-b")` is unchanged (still those same 5 rows).

Extend `PredictionService.test.ts` with the route-recent behaviors listed above, reusing the `makeMockRecentsRepo` helper from Task 1.
  </action>
  <acceptance_criteria>
    - `getPredictionsForStop("stop-1", { deviceId: "device-a", route: "1A" })` results in `upsertRecent` called twice, once with `("device-a", "stop", "stop-1")` and once with `("device-a", "route", "1A")` (PredictionService.test.ts).
    - `getPredictionsForStop("stop-1", { deviceId: "device-a" })` (no `route`) results in exactly one `upsertRecent` call, for `"stop"` only (PredictionService.test.ts) — proves RECENT-01's negative case.
    - A `FavoritesRecentsRepository.test.ts` case writing 6 distinct recents for `"device-a"` asserts `listRecents("device-a")` returns exactly 5 rows, with the first-written entity absent.
    - A `FavoritesRecentsRepository.test.ts` case re-upserting an existing top-5 recent asserts the row count stays at 5.
    - A `FavoritesRecentsRepository.test.ts` case asserts `"device-b"`'s 5 existing rows are unaffected when `"device-a"` writes a 6th recent.
    - `grep -c "DELETE FROM recents" src/server/api/repositories/FavoritesRecentsRepository.ts` returns `1`.
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- src/server/api/repositories/FavoritesRecentsRepository.test.ts src/server/api/services/PredictionService.test.ts</automated>
    <fails_when>non-zero exit code, or the vitest summary line shows a nonzero "failed" count</fails_when>
  </verify>
  <done>Route-recent logging fires only when an explicit route param is present (RECENT-01), bump-to-top dedup relies on the existing upsert (RECENT-03), and FavoritesRecentsRepository.upsertRecent enforces a hard cap of 5 combined recents per device with oldest-evicted-first (RECENT-04) — committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Recents list endpoint — combined, hydrated, viewedAt-DESC, device-scoped (RECENT-05)</name>
  <read_first>
    src/server/api/services/FavoritesService.ts and src/server/api/controllers/FavoritesController.ts (direct pattern templates per 07-CONTEXT.md D-08 — copy the listFavorites shape, substituting viewedAt for favoritedAt); src/server/api/routes/favoriteRoutes.ts and src/server/api/routes/favoriteRoutes.test.ts (router-mounting and supertest patterns to mirror exactly); src/server/api/models/PersistedEntity.ts (existing HydratedFavorite/RecentRecord — HydratedRecent is added as a sibling, no restructuring); src/server/api/routes/index.ts (mount pattern for the new `/recents` line); src/server/api/middleware/requireDeviceId.ts (reused as-is, not modified).
  </read_first>
  <files>src/server/api/models/PersistedEntity.ts, src/server/api/services/RecentsService.ts, src/server/api/services/RecentsService.test.ts, src/server/api/controllers/RecentsController.ts, src/server/api/controllers/RecentsController.test.ts, src/server/api/routes/recentRoutes.ts, src/server/api/routes/recentRoutes.test.ts, src/server/api/routes/index.ts</files>
  <behavior>
    - `listRecents(deviceId)` for a device with zero recent rows returns `[]`.
    - `listRecents(deviceId)` for a device with one route recent and one stop recent returns two entries, each shaped `{ entityType, viewedAt, entity }`, `entity` being the full `BusRoute`/`BusStop` object from `BusDataRepository`.
    - `listRecents(deviceId)` never re-sorts beyond what the repository already returns; the result is `viewedAt` DESC across both entity types combined (asserted against 3+ mixed-type fixtures with distinct timestamps).
    - `listRecents(deviceId)` silently omits an entry whose `entityId` no longer resolves via `BusDataRepository` — the returned array is shorter, not padded with a stub/null.
    - `GET /api/v1/recents` without `X-Device-Id` returns 400.
    - `GET /api/v1/recents` returns 200 and the service's array verbatim, including `200 []` for an empty list.
  </behavior>
  <action>
In `PersistedEntity.ts`, add `export interface HydratedRecent { entityType: EntityType; viewedAt: string; entity: BusRoute | BusStop; }` as a sibling export to the existing `HydratedFavorite` (the `BusRoute`/`BusStop` type imports already exist in this file from the Phase 6 addition).

Create `src/server/api/services/RecentsService.ts`: `export interface RecentsService { listRecents(deviceId: string): Promise<HydratedRecent[]>; }` and `export function createRecentsService(recentsRepository: FavoritesRecentsRepository, busDataRepository: BusDataRepository): RecentsService`. Implement the identical private `resolveEntity(entityType, entityId): BusRoute | BusStop | undefined` helper `FavoritesService.ts` defines, and `async function listRecents(deviceId: string): Promise<HydratedRecent[]> { const records = await recentsRepository.listRecents(deviceId); return records.map((record) => { const entity = resolveEntity(record.entityType, record.entityId); return entity ? { entityType: record.entityType, viewedAt: record.viewedAt, entity } : undefined; }).filter((entry): entry is HydratedRecent => entry !== undefined); }`. Return `{ listRecents }`.

Create `src/server/api/controllers/RecentsController.ts`: `export interface RecentsController { listRecents: RequestHandler; }`, `export function createRecentsController(service: RecentsService): RecentsController`. Copy `resolveErrorStatus`/`resolveErrorBody`/`resolveDeviceId` verbatim from `FavoritesController.ts` (404 for `NotFoundError`, 500 otherwise — even though `listRecents` never throws `NotFoundError` today, the shape stays consistent with the rest of the codebase). Implement `const listRecents: RequestHandler = async (req, res) => { try { const result = await service.listRecents(resolveDeviceId(req)); res.json(result); } catch (error: unknown) { res.status(resolveErrorStatus(error)).json(resolveErrorBody(error)); } };`. Return `{ listRecents }`.

Create `src/server/api/routes/recentRoutes.ts` following `favoriteRoutes.ts` exactly: `const recentsRepository = FavoritesRecentsRepository.getInstance(); const busDataRepository = BusDataRepository.getInstance(); const service = createRecentsService(recentsRepository, busDataRepository); const controller = createRecentsController(service); const router = Router(); router.use(requireDeviceId); router.get("/", controller.listRecents); export default router;`.

Modify `src/server/api/routes/index.ts`: add `import recentRoutes from "./recentRoutes";` alongside the existing four route imports, and `router.use("/recents", recentRoutes);` appended directly after the existing `router.use("/favorites", favoriteRoutes);` line.

Write `RecentsService.test.ts` mirroring `FavoritesService.test.ts`'s `describe("listFavorites", ...)` block exactly, substituting `viewedAt`/`RecentRecord`/`HydratedRecent`/`listRecents` for `favoritedAt`/`FavoriteRecord`/`HydratedFavorite`/`listFavorites`: empty list, mixed route+stop hydration, DESC-order preservation across 3+ fixtures, silent-skip of an unresolvable entity, and a 100-record no-cap case.

Write `RecentsController.test.ts` mirroring `FavoritesController.test.ts`'s `describe("listFavorites", ...)` block: 200 `[]` for an empty service result, 200 with the service's array verbatim, device id read from the `X-Device-Id` header, 500 on a generic service error.

Write `recentRoutes.test.ts` mirroring `favoriteRoutes.test.ts`'s `describe("GET /api/v1/favorites", ...)` block: `vi.mock("../services/RecentsService", ...)` returning a mocked `listRecents`, supertest `GET /api/v1/recents` — 400 without `X-Device-Id`, 200 `[]` for a mocked empty result, 200 with a mocked mixed route+stop array returned verbatim.
  </action>
  <acceptance_criteria>
    - `GET /api/v1/recents` without `X-Device-Id` returns 400 with `details: "X-Device-Id header is required"` (recentRoutes.test.ts).
    - `GET /api/v1/recents` with a valid header and a mocked empty `listRecents` result returns 200 `[]` (recentRoutes.test.ts).
    - `GET /api/v1/recents` with a mocked mixed route+stop result returns each entry shaped `{ entityType, viewedAt, entity }` (RecentsService.test.ts asserts the shape directly; recentRoutes.test.ts asserts the response body matches the service's return value).
    - A `RecentsService.test.ts` case where `busDataRepository.getRouteById`/`getStopById` returns `undefined` for one of several recent records asserts that record is absent from the returned array, with the array one shorter than the input record count.
    - A `RecentsService.test.ts` case with 3+ mixed-type fixtures having distinct `viewedAt` values asserts the returned array's `viewedAt` sequence is strictly descending.
    - `grep -n 'router.use("/recents", recentRoutes)' src/server/api/routes/index.ts` finds one match.
    - `grep -n "router.use(requireDeviceId)" src/server/api/routes/recentRoutes.ts` finds one match.
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- src/server/api/services/RecentsService.test.ts src/server/api/controllers/RecentsController.test.ts src/server/api/routes/recentRoutes.test.ts && bun run test && bun run build</automated>
    <fails_when>non-zero exit from any of the three commands, or the vitest summary line shows a nonzero "failed" count, or "error TS" appears in the build output</fails_when>
  </verify>
  <done>GET /api/v1/recents returns one combined, viewedAt-DESC-ordered array of hydrated {entityType, viewedAt, entity} entries (200 [] when empty), device-scoped via requireDeviceId, mounted at /recents — completing RECENT-05, with the full project test suite passing and the build compiling cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client -> Recents HTTP API (`GET /api/v1/recents`) | Untrusted `X-Device-Id` header crosses into `recentRoutes.ts`, validated by the reused `requireDeviceId` middleware |
| Client -> Predictions HTTP API (`GET /api/v1/predictions`) | Untrusted, now-optional `X-Device-Id` header crosses into `PredictionController`/`PredictionService` as an unvalidated *write* trigger, not merely a read scope (new in this phase — Phase 6's device id only gated explicit favorite actions) |
| `PredictionService` -> `FavoritesRecentsRepository` (SQLite, fire-and-forget write) | Internal; every prediction lookup carrying any device id value is now a write path into the `recents` table, triggered automatically rather than by an explicit user action |
| `RecentsController`/`RecentsService` -> `BusDataRepository` (existing, untouched) | Internal, already-trusted in-memory read boundary, reused for hydration |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Spoofing | `X-Device-Id` header on `/api/v1/predictions`, now triggering an automatic persistence write | medium | accept | Same root-cause acceptance as Phase 6's T-06-01 (`REQUIREMENTS.md` Out of Scope: "Device ID validation/signing/tokens... nothing sensitive is behind a favorites list yet") extended to recents; flagged at a higher severity than Favorites' equivalent because the write now fires passively on every prediction GET rather than requiring an explicit favorite action — an actor who guesses/knows another device's id can pollute that device's recents list merely by issuing prediction requests with that id in the header. No mitigation added this phase; `PERS-05` (v2 real accounts) remains the tracked long-term fix. |
| T-07-02 | Tampering / Injection | `upsertRecent`'s new eviction `DELETE` statement and the existing insert path in `FavoritesRecentsRepository.ts` | high | mitigate | The eviction statement uses exclusively named (`@deviceId`) bound parameters, matching the existing `upsertFavorite`/`upsertRecent`/`deleteFavorite` idiom — never string concatenation. `entityType` remains constrained by the controller's `EntityType` union type upstream and Phase 5's `CHECK (entity_type IN ('route','stop'))` table constraint. |
| T-07-03 | Denial of Service | Fire-and-forget recents write + eviction `DELETE` running on every prediction lookup that carries a device id | low | accept | The write is async and non-blocking (never delays the primary response per D-03) and reuses Phase 5's WAL journal mode + 5s `busy_timeout` connection settings; worst-case row growth per device is bounded at 5 by the eviction logic itself, so there is no unbounded storage growth vector. |
| T-07-04 | Information Disclosure | `GET /api/v1/recents` scoped by `X-Device-Id`, same disclosure profile as Favorites | low | accept | Same root cause/disposition as Phase 6's T-06-03; `listRecents` scopes strictly by `WHERE device_id = ?`, inherited unmodified from Phase 5. |
</threat_model>

<verification>
- `bun run test` passes (full suite, with `--typecheck`) after all three tasks.
- `bun run test:coverage` meets the 80% branches/functions/lines/statements thresholds.
- `bun run build` compiles cleanly.
- `bun run lint` exits 0 for new/modified files.
- `git diff --stat -- src/server/api/repositories/BusDataRepository.ts src/server/api/repositories/BusDataRepository.test.ts src/server/api/controllers/StopController.ts src/server/api/controllers/BusRouteController.ts src/server/api/services/StopService.ts src/server/api/services/BusRouteService.ts src/server/api/services/PredictionStreamService.ts src/server/api/controllers/PredictionStreamController.ts src/server/api/middleware/requireDeviceId.ts` is empty (non-regression — this phase touches only the prediction write-path, the recents repository/service/controller/routes, and routes/index.ts).
- Manual smoke check (optional): `bun run dev-server`, `curl "localhost:3000/api/v1/predictions?stop=<real stop id>&route=<real route id>" -H "X-Device-Id: dev-1"`, then `curl localhost:3000/api/v1/recents -H "X-Device-Id: dev-1"` and confirm both the stop and route appear, most-recent-first; repeat the predictions call 6 times across 6 distinct stops for the same device and confirm the recents list never exceeds 5 entries.
</verification>

<success_criteria>
- ROADMAP Phase 7 SC #1: A prediction lookup with an explicit `route` param auto-logs that route as a recent; an unfiltered stop-only lookup does not.
- ROADMAP Phase 7 SC #2: Every prediction lookup auto-logs its stop as a recent.
- ROADMAP Phase 7 SC #3: Re-viewing an already-recent route or stop bumps it to the top instead of duplicating it, and the combined recents list never exceeds 5 entries per device.
- ROADMAP Phase 7 SC #4: A client can list recents as one combined, type-tagged, hydrated array ordered most-recently-viewed-first.
- ROADMAP Phase 7 SC #5: Opening an SSE prediction stream never logs a recent, and the auto-logging side effect never delays or breaks the primary response, even on a persistence failure.
- RECENT-01 through RECENT-06 fully satisfied.
</success_criteria>

<output>
Create `.planning/phases/07-recents-routes-stops/07-01-SUMMARY.md` when done
</output>
