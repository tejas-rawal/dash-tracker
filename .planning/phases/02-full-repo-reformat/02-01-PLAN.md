---
phase: 02-full-repo-reformat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/config.json
  - src/server/api/controllers/BusRouteController.test.ts
  - src/server/api/controllers/BusRouteController.ts
  - src/server/api/controllers/PredictionController.test.ts
  - src/server/api/errors/index.test.ts
  - src/server/api/errors/index.ts
  - src/server/api/models/BusRoute.test.ts
  - src/server/api/models/BusRoute.ts
  - src/server/api/models/BusStop.test.ts
  - src/server/api/models/BusStop.ts
  - src/server/api/models/RouteDirection.test.ts
  - src/server/api/models/RouteDirection.ts
  - src/server/api/repositories/BusDataRepository.test.ts
  - src/server/api/repositories/BusDataRepository.ts
  - src/server/api/repositories/index.ts
  - src/server/api/routes/busRoutes.test.ts
  - src/server/api/routes/busRoutes.ts
  - src/server/api/routes/predictionRoutes.test.ts
  - src/server/api/routes/predictionRoutes.ts
  - src/server/api/services/BusRouteService.test.ts
  - src/server/api/services/BusRouteService.ts
  - src/server/api/services/PredictionService.test.ts
  - src/server/app.ts
  - src/server/config/axios.ts
  - src/server/config/environment.ts
  - src/server/config/index.ts
  - src/server/config/logger.ts
  - src/server/test/app.ts
  - src/server/test/setup.ts
  - tsconfig.json
  - vitest.config.mts
autonomous: true
requirements: [TOOL-04]

estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "Running biome check . --write a second time in succession (after the reformat commit) reports 'No fixes applied' with no file listed as Fixed — the reformat is idempotent and complete (ROADMAP Success Criterion 1)"
    - "bun run format (Biome-backed check) exits 0 against the full repository with no reported diffs (ROADMAP Success Criterion 2 / TOOL-04)"
    - "The reformat commit's file list is exactly the set of Biome-touched tracked files from this plan's files_modified — it does NOT include CLAUDE.md or .planning/phases/01-consolidate-lint-format-tooling/01-PATTERNS.md (D-03)"
    - "bun run build and bun run test both exit 0 after the reformat, unchanged from their pre-reformat pass/fail state, confirming the change was purely cosmetic (ROADMAP Success Criterion 4)"
    - "biome.json is byte-identical before and after this phase's commit — no formatter/linter rule changes (D-02)"
    - "The 10 pre-existing Biome warnings (axios.ts baseURL naming, 8 test-file useFilenamingConvention warnings, BusRouteController.test.ts useThrowOnlyError warnings) still exist unchanged after the reformat — no code edits, renames, or biome.json suppressions were applied to silence them (D-02)"
  artifacts: []
  key_links:
    - "package.json scripts.format / scripts.format:write -> biome check . / biome check . --write (unchanged from Phase 1, now exercised clean across the full repo)"
    - "git add -u -> git commit -> single isolated reformat commit containing only Biome-touched tracked files, separate from Phase 1's tooling/config commits"
  prohibitions:
    - "Do not modify biome.json in this phase's commit (formatter/linter blocks locked from Phase 1)"
    - "Do not use biome check . --write --unsafe — safe fixes only, per D-01"
    - "Do not rename any *.test.ts file or edit the residual-warning lines (axios.ts:7:45, the 8 useFilenamingConvention test files, BusRouteController.test.ts:151/242) to silence Biome warnings, per D-02"
    - "Do not stage CLAUDE.md or .planning/phases/01-consolidate-lint-format-tooling/01-PATTERNS.md in this commit — never use git add -A or git add . for this commit, per D-03"
    - "Do not modify package.json scripts, dependencies, or any other Phase-1-locked tooling config"
---

<objective>
Run Biome's formatter (safe fixes only) across the entire dash-tracker repository so `bun run format`/`bun run lint` exit 0 with zero outstanding diffs repo-wide, and land the resulting diff as a single, isolated, purely-cosmetic commit — separate from Phase 1's tooling/config commits — per CONTEXT.md decisions D-01 through D-03.

Purpose: closes TOOL-04 — the full codebase now conforms to the Biome-only formatter established in Phase 1, proving the tooling migration is complete and behavior-neutral.
Output: ~30 already-tracked source/config files reformatted (double-quote/4-space/120-width per `biome.json`, unchanged); one new git commit containing only that formatting diff. No new files, functions, exports, or config keys are introduced — see `<artifacts_produced>`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-full-repo-reformat/02-CONTEXT.md
@.planning/phases/01-consolidate-lint-format-tooling/01-CONTEXT.md
@.planning/phases/01-consolidate-lint-format-tooling/01-VERIFICATION.md
@biome.json
@package.json
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Reformat the full repo with Biome and land it as an isolated commit</name>
  <files>All ~30 Biome-touched tracked files listed in this plan's `files_modified` frontmatter (src/server/**/*.ts, tsconfig.json, vitest.config.mts, .planning/config.json). No file outside that list should be touched; biome.json and package.json must remain byte-identical.</files>
  <read_first>.planning/phases/02-full-repo-reformat/02-CONTEXT.md, .planning/phases/01-consolidate-lint-format-tooling/01-CONTEXT.md, .planning/phases/01-consolidate-lint-format-tooling/01-VERIFICATION.md, biome.json, package.json</read_first>
  <precondition>git status --porcelain shows only two untracked entries (CLAUDE.md, .planning/phases/01-consolidate-lint-format-tooling/01-PATTERNS.md) and zero modified tracked files before this task begins, so the resulting diff is attributable solely to Biome's --write pass.</precondition>
  <action>
Confirm the precondition with `git status --porcelain` — only the two untracked docs should appear, no modified tracked files.
Run `biome check . --write` (safe fixes only — no `--unsafe`, per D-01) to reformat the full repository. This is expected to touch the ~30 tracked files listed in this plan's `files_modified` and bring the repo from 35 errors/10 warnings down to 0 errors/10 warnings (verified directly during Phase 2 discussion, see 02-CONTEXT.md code_context).
Run `biome check . --write` a second time in succession. It must report `No fixes applied` with no file listed as `Fixed` — this proves the reformat is idempotent and complete (ROADMAP Success Criterion 1).
Run `bun run format` and confirm it exits 0 with no reported diffs across the full repository (ROADMAP Success Criterion 2 / TOOL-04). The 10 residual warnings from D-02 may still print but must not cause a non-zero exit.
Run `bun run build` and confirm it exits 0. Run `bun run test` and confirm it exits 0 with the same tests passing as before this task (formatting must not change behavior). Together these confirm ROADMAP Success Criterion 4 — the reformat was purely cosmetic.
Do not touch `biome.json` — its `formatter`/`linter` blocks and `files.ignore` stay byte-identical to the Phase-1-locked values.
Do not edit, rename, or otherwise touch the 10 residual-warning locations documented in D-02: `src/server/config/axios.ts:7:45` (`useNamingConvention` on Axios's own `baseURL` option name), the 8 `*.test.ts` files flagged by `useFilenamingConvention` (the project's established `PascalCase.test.ts` convention is intentional), and `BusRouteController.test.ts:151,242` (`useThrowOnlyError` + suppression/unused warnings on intentional non-Error throw test cases). These must remain exactly as Biome's safe `--write` leaves them — no manual code edits, no file renames, no `biome.json` rule suppressions.
Stage only the Biome-touched tracked files with `git add -u` (stages modifications to already-tracked files only; it never adds new/untracked files) — never `git add -A` or `git add .`, which would sweep in the two untracked planning docs, per D-03.
Before committing, verify the staged set with `git diff --cached --name-only`: it must list only files from this plan's `files_modified` frontmatter and must NOT include `CLAUDE.md` or `.planning/phases/01-consolidate-lint-format-tooling/01-PATTERNS.md`.
Commit with `git commit -m "style(02-01): reformat full repository with biome (formatting only, no logic changes)"` — wording is Claude's discretion per CONTEXT.md, as long as it makes unambiguous this is a Biome-formatter-only commit with zero logic edits, isolated from Phase 1's tooling/config commits (ROADMAP Success Criterion 3).
After committing, confirm via `git status --porcelain` that `CLAUDE.md` and `.planning/phases/01-consolidate-lint-format-tooling/01-PATTERNS.md` remain untracked and uncommitted — they are intentionally excluded and left for the user to commit separately.
  </action>
  <verify>
    <automated>OUT=$(biome check . --write 2>&1); EC=$?; echo "$OUT" | grep -q "No fixes applied" && ! echo "$OUT" | grep -q "Fixed " && [ "$EC" = "0" ] && bun run format && bun run build && bun run test && [ -z "$(git diff HEAD~1 HEAD -- biome.json)" ] && [ -z "$(git diff HEAD~1 HEAD -- package.json)" ] && [ -z "$(git diff-tree --no-commit-id --name-only -r HEAD | grep -E '^(CLAUDE\.md|\.planning/phases/01-consolidate-lint-format-tooling/01-PATTERNS\.md)$')" ] && [ "$(git status --porcelain | grep -cE '^\?\? (CLAUDE\.md|\.planning/phases/01-consolidate-lint-format-tooling/01-PATTERNS\.md)$')" = "2" ] && echo "PHASE 2 VERIFY OK"</automated>
  </verify>
  <acceptance_criteria>
    - Second `biome check . --write` run (post-commit) reports "No fixes applied" with no file listed as "Fixed" — idempotent (ROADMAP Success Criterion 1)
    - `bun run format` exits 0 against the full repo (ROADMAP Success Criterion 2 / TOOL-04)
    - `bun run build` exits 0 and `bun run test` exits 0 — unchanged from the pre-reformat pass/fail state (ROADMAP Success Criterion 4)
    - `biome.json` is byte-identical to its pre-commit value: `git diff HEAD~1 HEAD -- biome.json` is empty
    - `package.json` is byte-identical to its pre-commit value: `git diff HEAD~1 HEAD -- package.json` is empty — scripts locked from Phase 1, untouched
    - The new commit (`git diff-tree --no-commit-id --name-only -r HEAD`) contains only Biome-touched tracked files and does NOT include `CLAUDE.md` or `.planning/phases/01-consolidate-lint-format-tooling/01-PATTERNS.md` (D-03)
    - `CLAUDE.md` and `.planning/phases/01-consolidate-lint-format-tooling/01-PATTERNS.md` remain untracked (`git status --porcelain` shows `??` for both) after the commit — excluded, left for the user
    - The 10 pre-existing residual warnings (axios.ts naming, 8 test-file naming-convention warnings, BusRouteController.test.ts useThrowOnlyError) are untouched — no renames, no code edits, no biome.json suppressions (D-02)
    - The reformat is a single new commit on top of Phase 1's final commit, containing only formatting changes — no functional/logic edits (ROADMAP Success Criterion 3)
  </acceptance_criteria>
  <reversibility rating="reversible">A single mechanical formatting commit — fully undone via `git revert <sha>` (or `git reset --hard HEAD~1` before pushing); no data migration, no external service state, no irreversible side effect. This phase never warrants a decision checkpoint.</reversibility>
  <done>The full repository passes `bun run format`/`bun run lint` with 0 errors (10 pre-existing warnings untouched), a second `biome check . --write` reports zero changes, `bun run build` and `bun run test` both still pass, and the entire diff is committed as one isolated, purely-cosmetic commit that excludes CLAUDE.md and 01-PATTERNS.md.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts This Phase Produces

None. This is a pure reformatting pass over already-existing tracked files — no new files, functions, exports, config keys, or scripts are introduced. The only change is whitespace/quote-style formatting (per `biome.json`'s existing formatter block: 120 char line width, 4-space indent, double quotes) applied to the ~30 files listed in this plan's `files_modified` frontmatter, landed as a single new commit on top of Phase 1's final commit. `biome.json` and `package.json` are unchanged. Downstream drift verification should expect zero new symbols from this phase.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Developer/CI toolchain → tracked source files | `biome check . --write` mutates already-tracked source/config files in place via safe fixes only; no runtime code path, API surface, or user-facing behavior is touched by this phase |
| Local git staging → commit history | `git add` selects which working-tree changes become part of the permanent, shared commit history — the isolation requirement (Success Criterion 3) depends entirely on staging discipline |

This phase installs no new packages — the npm/pip/cargo package-legitimacy gate does not apply.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-02-01 | Tampering | `git add` staging scope for the reformat commit | medium | mitigate | Using `git add -A`/`git add .` would sweep the two untracked planning docs (`CLAUDE.md`, `01-PATTERNS.md`) into the "isolated" reformat commit, violating D-03 and Success Criterion 3. Mitigation: the task's `<action>` mandates `git add -u` (stages modifications to already-tracked files only, never untracked files) and the `<verify>`/`<acceptance_criteria>` cross-check the committed file list and post-commit `git status` to confirm both docs remain excluded. |
| T-02-02 | Tampering | `biome check . --write --unsafe` (not used) | low | accept | An unsafe fix pass could alter code behavior rather than just formatting. D-01 restricts this task to safe-only `--write`; the action explicitly forbids `--unsafe`. Accepted risk is negligible since safe fixes are formatting-only by Biome's own definition, and the idempotency check plus `bun run test` would surface any behavioral drift. |
| T-02-03 | Tampering | Residual-warning locations (axios.ts naming, 8 test-file names, useThrowOnlyError) | low | mitigate | Risk of "cleaning up" these warnings via renames/edits/`biome.json` suppressions during the reformat, which would be an undisclosed scope expansion beyond D-02. Mitigation: `<action>` explicitly lists and forbids touching each location; `<acceptance_criteria>` verifies all 10 warnings persist unchanged after the commit. |

No other STRIDE categories (Spoofing, Repudiation, Information Disclosure, Elevation of Privilege) apply — this phase edits static, already-tracked files in a private git repository with no authentication, request-handling, or data-exposure surface.
</threat_model>

<verification>
Run after Task 1 completes, to confirm the full slice holds together against all four ROADMAP success criteria:

1. `biome check . --write` run a second time reports "No fixes applied" with no file listed as "Fixed" — idempotent (Success Criterion 1).
2. `bun run format` exits 0 against the full repository (Success Criterion 2 / TOOL-04).
3. `git diff-tree --no-commit-id --name-only -r HEAD` (the reformat commit) contains only formatting-touched files, excludes `CLAUDE.md` and `01-PATTERNS.md`, and is a single commit separate from any Phase 1 commit (Success Criterion 3).
4. `bun run build` exits 0 and `bun run test` exits 0, unchanged from pre-reformat behavior (Success Criterion 4).
5. `git diff HEAD~1 HEAD -- biome.json package.json` is empty — no tooling/config drift snuck into this commit.
6. `bun run lint 2>&1 | grep -cE "useNamingConvention|useFilenamingConvention|useThrowOnlyError"` reports at least 10 matches — the D-02 residual warnings still exist, confirming they were not silently fixed.
</verification>

<success_criteria>
- Running `biome check . --write` (or `bun run format:write`) a second time in succession reports zero remaining changes — the reformat is idempotent and complete (ROADMAP Success Criterion 1).
- `bun run format` (Biome-backed check) exits 0 against the full repository with no reported diffs (ROADMAP Success Criterion 2 / TOOL-04).
- The mass reformat is committed on its own, containing only formatting changes and no functional/logic edits — separate from the Phase 1 tooling/config commits (ROADMAP Success Criterion 3).
- `bun run build` and `bun run test` both still pass unchanged after the reformat, confirming the change was purely cosmetic (ROADMAP Success Criterion 4).
</success_criteria>

<output>
Create `.planning/phases/02-full-repo-reformat/02-01-SUMMARY.md` when done
</output>
