---
quick_id: 260915-fc8
mode: quick
---

# Plan: Confirm predictions endpoint uses Swiftly real-time API; add vehicle positions endpoint

## Task 1: Confirm predictions endpoint already calls Swiftly's real-time predictions API

- files: src/server/api/services/PredictionService.ts, .env.example
- action: No code change expected — this is a verification-only task. `PredictionService.buildDashApiUrl`
  already builds `/real-time/${agency}/predictions?stop=...&route=...&number=...` and calls it via the
  shared `axios` instance, whose `baseURL` is `environment.dashApi.baseUrl` (`DASH_API_BASE_URL`, set to
  `https://api.goswift.ly` in `.env.example`) with `Authorization` set to `DASH_API_KEY`
  (`src/server/config/axios.ts`). `api.goswift.ly` is Swiftly's API host, and
  `/real-time/{agency-key}/predictions` is Swiftly's real-time predictions REST path (the same
  `/real-time/{agency-key}/<resource>` family the new `/real-time/{agency}/vehicles` call in Task 2 also
  uses) — so this endpoint is already correct per
  https://swiftly-inc.stoplight.io/docs/swiftly-docs/6zpcgvbu5wbb3-swiftly-api-reference/operations/list-real-time-predictions.
  Read the file, confirm the URL-building logic matches this description, and make no edits.
- verify: `grep -c 'real-time/${agency}/predictions' src/server/api/services/PredictionService.ts` returns 1;
  `grep -c 'https://api.goswift.ly' .env.example` returns 1
- done: Confirmed `PredictionService` already calls Swiftly's real-time predictions endpoint correctly; no
  source changes made.

## Task 2: Add Vehicle model and VehicleService fetching from Swiftly's real-time vehicles API

- files: src/server/api/models/Vehicle.ts, src/server/api/models/index.ts, src/server/api/services/VehicleService.ts, src/server/api/services/VehicleService.test.ts
- action: This is the new/uncertain integration point — build and test it before wiring the HTTP layer in
  Task 3. Create `src/server/api/models/Vehicle.ts` following the exact `Prediction.ts` split (DASH-shaped
  types kept separate from the service response type, mapped explicitly in the service): export
  `DashVehicle` (fields: `id`, `routeId`, `routeShortName`, `tripId`, `directionId`, `headsign`, `lat`,
  `lon`, `heading`, `speed`, `lastUpdated` — all `string` except `lat`/`lon`/`heading`/`speed`/`lastUpdated`
  as `number`), `DashVehiclesApiResponse` (`{ success: boolean; route: string; data: { agencyKey: string;
  vehicles: DashVehicle[] } }`, mirroring `DashApiResponse`'s envelope shape), `VehiclePosition` (same field
  set as `DashVehicle`, the public response shape), `VehiclePositionsResponse` (`{ success: boolean;
  generatedAt: string; data: { agencyKey: string; vehicles: VehiclePosition[] } }`, mirroring
  `StopPredictionsResponse`), and `VehicleOptions` (`{ route?: string }`). NOTE: the Swiftly vehicles docs
  page (https://swiftly-inc.stoplight.io/docs/swiftly-docs/6zpcgvbu5wbb3-swiftly-api-reference/operations/list-real-time-vehicles)
  is a JS-rendered Stoplight page that could not be fetched for exact field names during planning (no live
  `DASH_API_KEY` available either) — this field set is a best-effort inference from standard real-time
  vehicle-position conventions and from this project's own `Prediction.ts`/`PredictionService.ts` sibling
  endpoint. The DASH/service-type separation already isolates exactly this risk (same mitigation this repo
  used for the alerts payload in Phase 8, per STATE.md) — if the live field names differ, only
  `DashVehicle` and the mapping function below need to change, not the public `VehiclePosition` shape or
  any downstream layer. Add `export * from "./Vehicle";` to `src/server/api/models/index.ts` (append after
  the `StopDiscovery` line, keeping alphabetical order). Create
  `src/server/api/services/VehicleService.ts` exporting `VehicleService` interface (`{
  getVehiclePositions(options?: VehicleOptions): Promise<VehiclePositionsResponse> }`) and factory
  `createVehicleService(): VehicleService` (no repository dependency — unlike `PredictionService`, there is
  no required identifying param to validate against `BusDataRepository`). Mirror `PredictionService.ts`'s
  structure: `buildDashApiUrl(options)` builds `` `/real-time/${agency}/vehicles` `` from
  `environment.dashApi.agency`, appending `?route=<value>` via `URLSearchParams` only when
  `options.route !== undefined` (mirror `PredictionService`'s exact `!== undefined` check, including its
  known empty-string quirk, for consistency with the sibling endpoint); `fetchFromDashApi(options)` calls
  `axios.get(url)` and logs via `logger.info` before the call (same style as
  `PredictionService.fetchFromDashApi`); `mapToVehiclePositions(vehicles: DashVehicle[])` maps 1:1 to
  `VehiclePosition[]`; `getVehiclePositions(options = {})` calls `fetchFromDashApi`, throws
  `UpstreamApiError("DASH API returned success: false for vehicle positions")` when
  `dashResponse.success` is false, otherwise returns `{ success: true, generatedAt: new
  Date().toISOString(), data: { agencyKey: dashResponse.data.agencyKey, vehicles:
  mapToVehiclePositions(dashResponse.data.vehicles) } }`. Create
  `src/server/api/services/VehicleService.test.ts` mocking `../../config` exactly like
  `PredictionService.test.ts` (`vi.mock("../../config", ...)` with `axios: { get: vi.fn() }`,
  `environment.dashApi`, `logger`), with a `makeDashVehicle`/`makeDashVehiclesApiResponse` factory pair
  analogous to `makeDashPredictionData`/`makeDashApiResponse`, covering: calls DASH API with the agency key
  in the URL; omits the `route` param from the URL when not provided; forwards the `route` param when
  provided; maps `DashVehicle[]` to `VehiclePosition[]` (spot-check one field per axis, e.g. `id`, `lat`,
  `heading`); returns an empty `vehicles` array when the DASH API returns none; includes a `generatedAt`
  ISO-8601 timestamp; throws `UpstreamApiError` when `success: false`; propagates a thrown error when the
  axios call rejects.
- verify: `bun run test -- src/server/api/services/VehicleService.test.ts`
- done: `VehicleService.getVehiclePositions` builds the correct Swiftly URL (with/without the `route`
  filter), maps `DashVehicle[]` to `VehiclePosition[]`, throws `UpstreamApiError` on `success: false`, and
  all `VehicleService.test.ts` cases pass.

## Task 3: Add VehicleController, vehicleRoutes, and wire GET /api/v1/vehicles

- files: src/server/api/controllers/VehicleController.ts, src/server/api/controllers/VehicleController.test.ts, src/server/api/routes/vehicleRoutes.ts, src/server/api/routes/index.ts
- action: Complete the vertical slice from Task 2 by exposing it over HTTP, mirroring
  `PredictionController.ts`/`predictionRoutes.ts` exactly. Create
  `src/server/api/controllers/VehicleController.ts` exporting `VehicleController` interface (`{
  getVehiclePositions: RequestHandler }`) and factory `createVehicleController(service: VehicleService):
  VehicleController`. Reuse `PredictionController`'s `resolveErrorStatus`/`resolveErrorBody` helper shapes
  verbatim (same `NotFoundError`→404/"Not Found", `UpstreamApiError`→502/"Bad Gateway", default→500/"Request
  Failed" mapping — `NotFoundError` is unreachable today since `VehicleService` never throws it, but keep
  the branch for consistency with every other controller in this codebase). `getVehiclePositions` reads
  `route` from `req.query as Record<string, string | undefined>` (no required params — unlike
  `PredictionController`, `route` is optional and there is no 400 case), calls
  `service.getVehiclePositions({ route })`, responds `res.json(result)` on success, and on catch responds
  `res.status(resolveErrorStatus(error)).json(resolveErrorBody(error))`. Create
  `src/server/api/controllers/VehicleController.test.ts` mirroring `PredictionController.test.ts`'s
  `makeMockRes`/`makeMockReq`/`makeMockService` helpers, covering: calls the service with `route: undefined`
  when no query param is present; calls the service with the provided `route` value when present; responds
  200 with the service's response body on success; does not call `res.status` on success; responds 502 with
  a "Bad Gateway" body when the service throws `UpstreamApiError`; responds 500 with a "Request Failed" body
  when the service throws a generic `Error`. Create `src/server/api/routes/vehicleRoutes.ts` mirroring
  `predictionRoutes.ts`: instantiate `createVehicleService()` and `createVehicleController(service)` at
  module scope, `router.get("/", controller.getVehiclePositions)`, default-export the router. Edit
  `src/server/api/routes/index.ts`: replace the two placeholder comment lines (`// Add other domain routes
  here` and `// router.use('/vehicles', vehicleRoutes);`) with a real `import vehicleRoutes from
  "./vehicleRoutes";` (grouped with the other route imports, alphabetically after `stopRoutes`) and
  `router.use("/vehicles", vehicleRoutes);` (appended after the existing `router.use("/recents",
  recentRoutes);` line, matching the file's existing insertion-order convention).
- verify: `bun run test -- src/server/api/controllers/VehicleController.test.ts`; `grep -c
  'router.use("/vehicles", vehicleRoutes)' src/server/api/routes/index.ts` returns 1
- done: `GET /api/v1/vehicles` is registered and reachable, returns live Swiftly vehicle-position data
  (optionally filtered by `?route=`), maps upstream/generic errors to 502/500, and all
  `VehicleController.test.ts` cases pass.
