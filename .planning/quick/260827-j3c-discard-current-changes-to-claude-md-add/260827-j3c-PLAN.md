---
quick_id: 260827-j3c
mode: quick
---

# Plan: Add Engineering Principles section; remove Core Value/Constraints

## Task 1: Update AGENTS.md

- files: AGENTS.md
- action: Remove the "Core value" and "Constraints" sections (lines 5-10). Add a new
  "## Engineering Principles" section at the end of the file (mirroring CLAUDE.md's
  placement as the final section) with the seven bullet points supplied by the user.
- verify: `grep -c "Core value" AGENTS.md` returns 0; `grep -c "Constraints" AGENTS.md` returns 0;
  `grep -c "Engineering Principles" AGENTS.md` returns 1
- done: AGENTS.md has no Core Value/Constraints sections and ends with Engineering Principles

## Task 2: Verify CLAUDE.md already matches target state

- files: CLAUDE.md
- action: Root CLAUDE.md already contains the "## Engineering Principles" section (as the
  final section) and has no "Core Value" or "Constraints" sections — confirm no edit needed.
- verify: `grep -c "Engineering Principles" CLAUDE.md` returns 1; `grep -ci "core value" CLAUDE.md`
  and `grep -ci "^### Constraints\|^## Constraints" CLAUDE.md` return 0
- done: Confirmed CLAUDE.md is already compliant; no changes made
